# 车间与设备管理模块设计

## 目标

让企业管理员能够在云端工作台中创建车间，并在两种设备接入方式之间做出清楚选择：

1. Windows 现场采集器扫描、验证并上传设备配置，适合批量接入。
2. 管理员手动填写固定 IP、TCP 端口和 Slave ID，适合少量已知设备。

无论使用哪种方式，设备都进入同一份资产清单、同一套监测页面和同一条历史数据链路。

本机并不是特殊的“后台机器”，它与 Windows 电脑一样都是采集器节点。管理员在云端看到本机和其他远程采集器的同一份节点清单；本机页面只允许维护当前节点，避免一台边缘凭据越权修改其他节点。

## 当前兼容模型与边界

当前数据库沿用 `sites` 表承载采集器节点，业务上明确为“一条 `site_id` 对应一个采集器节点”。每台设备只归属一个节点，每个节点同一时刻只允许一个稳定 `collector_instance_id` 持有 90 秒活动租约。这样可以在不破坏现有数据的前提下，阻止两台电脑重复轮询同一设备。

这是一种兼容性落地，不代表“物理园区”和“采集器”永远是同一实体。如果后续确认一个物理站点需要主备或多台采集器协作，应新增独立 `collectors` 表和设备租约，形成 `客户 → 物理站点 → 采集器 → 车间 → 设备`，不能继续让多台机器共用同一 `site_id`。

## 角色边界

| 角色 | 可以做什么 | 不应该做什么 |
|---|---|---|
| 企业管理员 | 创建车间、手动登记设备、选择负责采集器、维护名称和报警策略 | 把“保存成功”当成设备已连接；从云端直接扫描内网 |
| 现场技术人员 | 在 Windows 采集器发现设备、测试连接、诊断网络、维护令牌 | 管理其他客户或其他站点 |
| 只读用户 | 查看实时、历史和报警 | 创建车间、添加设备或修改策略 |
| 平台管理员 | 开通客户、站点和账号，审计配置变更 | 默认进入客户内网或读取现场令牌 |

历史版本的 `customer` 角色继续按企业管理员处理，以免升级后现有账号突然失去权限；新账号默认使用更明确的 `customer_admin`。只读账号使用 `viewer`。

## 为什么云端手动添加后不能立即显示在线

设备使用 `192.168.x.x`、`10.x.x.x` 或 `172.16-31.x.x` 等现场私网地址。云端服务器无法也不应该主动进入车间网络。因此：

- 配置写入成功，只能证明资产和期望配置已保存。
- 现场采集器拉取到配置，只能证明下发链路可用。
- 采集器读到设备并上传首条 `source=device` 数据，才证明设备真实在线。

这三个事实不能合并为同一个绿色“成功”。

## 管理员操作流程

### 创建车间

1. 进入“场所设备”。
2. 点击“新建车间”。
3. 填写唯一的业务名称，例如“二楼灌装车间”。
4. 系统创建车间，并自动附加完整的默认报警策略。
5. 创建完成后直接进入设备添加步骤。

首版不提供物理删除车间。历史数据和报警记录已经引用车间标识，直接删除会破坏追溯。后续如需下线，应该增加“归档”状态而不是删除。

### Windows 采集器发现

1. 现场人员在安装电脑打开本机采集器。
2. 扫描经过授权的私有网段。
3. 对候选设备做 Modbus 和真实寄存器读取验证。
4. 选择车间、命名并保存。
5. 采集器主动向云端上传配置与真实读数。

这是多设备场景的推荐方式，因为它能在保存前发现地址、端口或协议错误。

### 管理员手动登记

1. 选择所属车间。
2. 多站点客户选择与设备同网的现场采集器；单站点自动选中。
3. 填写设备名称和私有 IPv4 地址。
4. 必要时展开高级参数修改 TCP 端口和 Slave ID；默认值为 `502` 和 `1`。
5. 保存后状态为“待采集器确认”。
6. 首条真实读数到达后自动变为“在线”；曾有真实读数但已过期则显示“离线”。

## 状态模型

| 状态 | 判定依据 | 管理员理解 |
|---|---|---|
| 待采集器确认 | 从未收到该设备的真实读数 | 配置已登记，但连接尚未被证明 |
| 在线 | 45 秒内收到 `source=device` 的有效读数 | 现场连接和采集链路正常 |
| 离线 | 曾收到真实读数，但当前已超过新鲜度窗口 | 连接中断或采集器未继续上传 |
| 模拟数据 | 只有 `source=demo` 数据 | 不用于证明设备在线，不用于生产判断 |

采集器节点另行显示“待激活、在线、离线、需升级心跳、需要处理”，并分别展示最后心跳、最后真实采集、设备在线数、待上传、磁盘、版本和配置应用版本。设备离线、采集器离线与云端同步失败不能合并为一个状态。

## 数据与接口

### 云端

- `GET /api/admin/sites`：列出当前客户可用的现场采集器/站点和配置版本。
- `POST /api/admin/collectors`：云端管理员创建远程采集器节点，原始接入密钥只返回一次。
- `POST /api/admin/cleanrooms`：管理员创建车间。
- `POST /api/admin/devices`：管理员手动登记设备。
- `POST /api/v1/edge/cleanrooms`：单站点本机控制台创建车间并同步云端。
- `POST /api/v1/edge/devices`：本机控制台登记设备，站点由边缘令牌强制确定。
- `POST /api/v1/edge/heartbeat`：采集器上报版本、设备状态、最后采集、积压、磁盘和配置应用回执。

设备端点在同一站点内必须唯一：`site_id + host + tcp_port + slave`。IP 不是设备永久身份，设备仍使用独立、稳定的 `device_id`。

### 本机独立运行

没有配置云端时，同样的管理员接口写入本机 SQLite。后台监测线程会读取新配置并开始轮询。配置云端后，本机管理请求先写云端，再原子应用云端返回的新配置版本，避免下一次拉取覆盖本机改动。

## 安全与一致性

- 所有管理员接口按登录用户的 `customer_id` 做服务端租户隔离。
- `viewer` 即使直接调用 API 也会得到 `403`，不能只依赖隐藏按钮。
- 设备地址只允许私有 IPv4，端口限制为 `1-65535`，Slave ID 限制为 `1-247`。
- 云端边缘接口不信任客户端提供的站点，始终使用边缘令牌绑定的 `site_id`。
- 采集器使用本地持久化的实例 ID；云端活动租约拒绝同一节点的第二个实例，避免重复采集。
- 原始接入密钥仅在创建节点时返回一次，不进入日志、审计详情或采集器清单接口。
- Windows 采集器上传、更新、停用和拉回的设备清单都限定在令牌绑定站点，不能覆盖其他站点资产。
- 车间名、同车间设备名和同站点设备端点都有唯一性约束。
- 创建请求按客户串行化，避免两个管理员同时提交时绕过容量和重复检查。
- 新建车间和设备写入配置审计，记录操作者类型、目标、时间和关键参数。
- 多站点客户不能从某一台本机采集器创建全客户共享车间，必须由云端管理员创建，避免站点越权。

## 已知边界与下一步

当前已完成节点创建、手动添加、心跳与配置回执的代码闭环。下一阶段应按优先级补充：

1. 接入密钥轮换、吊销、遗失处理和二维码/短期激活码，替代人工复制长期令牌。
2. 设备编辑、迁移车间、迁移采集器、软停用，以及车间归档。
3. 操作审计查询页面，而不只是后台留痕。
4. 稳定硬件身份（序列号/型号指纹），处理设备更换 IP 而不产生重复资产。
5. 真正拆分物理站点与采集器实体，并为主备采集设计设备级租约。

不建议现在直接增加“删除车间”和“云端测试 IP”按钮：前者会破坏历史引用，后者在网络结构上通常不可达，即使偶尔可达也会扩大现场网络暴露面。

## 验收标准

- 管理员三步内完成车间创建并进入设备添加。
- 公网 IP、非法端口、非法 Slave ID、重复名称和重复端点均无法保存。
- 只读用户页面无入口，直接 API 调用也被拒绝。
- 手动保存后明确显示“待采集器确认”，不显示“在线”。
- 模拟数据不能把设备升级为在线。
- Windows 采集器拉取新版本后能看到新车间和设备。
- 第二个实例尝试占用同一节点时被拒绝，不能开始重复采集。
- 配置应用失败时保留上一版本，并在云端显示错误而不是继续标绿。
- 首条真实读数上传后，页面状态、设备 ID、时间和值能够与本机记录核对一致。
- 桌面端与 390px 手机端无横向溢出，主要操作不依赖鼠标悬停。
