# HawkHive Windows 采集器自动接入方案

## 最终操作流程

一台 Windows 电脑对应一个“采集器节点”，但同一节点可以负责多个车间中的多台设备。物理位置无法从电脑名或 IP 地址可靠推断，因此云端管理员只确认三类业务信息：电脑报到时核对并修改**采集器名称**；设备发现后选择**所属车间**并修改**设备名称**。

1. 云端管理员下载一次“本客户通用 Windows 采集器”ZIP；不需要预先新建节点，也不需要填写电脑数量。
2. 同一个 ZIP 可复制到本客户的多台 Windows 电脑。现场人员完整解压、双击 EXE，并同意一次管理员权限提示。
3. 每台电脑生成自己的安装身份和申请密钥，启动后自动出现在云端“待加入电脑”列表；此时没有数据上传权限。
4. 云端管理员识别电脑，只需决定加入或拒绝；加入时可保留电脑名或修改采集器名称。
5. 该电脑取得自己独立的长期上传凭据，注册开机任务并扫描所有活动的私有 IPv4 网卡。
6. 只有通过 DPC8001-G 协议及颗粒单位校验的候选设备才进入云端“待分配设备”。
7. 云端管理员为候选设备选择车间并修改名称；保存后配置自动下发，开始采集和上传。

现场人员不需要填写云端地址、节点 ID、令牌、设备 IP、TCP 端口或 Slave ID，也不需要抄录配对码。安装 ZIP 在同一客户内可重复使用；包内登记令牌只能创建“待加入”记录，不能读取、上传或取得其他电脑的凭据。待加入记录每次报到续期 24 小时。管理员批准后，长期上传凭据按电脑隔离并通过 Windows 机器级 DPAPI 加密。

如果通用安装包误发或泄露，可调用受管理员权限保护的安装包轮换接口，使旧包及尚未确认的申请立即失效；已经确认并运行的采集器不受影响。旧版“一节点一个一次性激活包”接口仅保留兼容，不再出现在默认操作流程中。

## 自动发现边界

- 自动枚举全部活动私有 IPv4 网卡；大于 `/24` 的网段只扫描本机地址所在的 `/24`，单网段最多 254 个地址。
- 当前按通讯协议默认探测 Modbus TCP `502` 端口和 Slave ID `1`。
- 校验实时颗粒寄存器、固件寄存器和颗粒单位寄存器；颗粒单位必须为协议规定的 `PCS/28.3L`。
- 已分配设备的 IP 会从后续扫描中排除，避免扫描连接与持续采集争用设备。
- 同一候选 IP 可以分配到任一车间；同一 IP/端口/Slave 在同一采集器下只能启用一次。

以下情况不能声称“全自动发现”：设备位于未直接连接的路由/VLAN 子网、使用非 502 端口、使用非 1 Slave ID、被厂家上位机独占连接，或被防火墙阻止。此时需要客户提供网络范围或厂家提供连接能力，而不是扩大到无边界扫描。

## Windows 后台运行和本地数据

- EXE 内含运行时，目标电脑不需要 Python。
- 首次运行安装到 `C:\Program Files\HawkHive\DPC8001Collector`，数据保存到 `C:\ProgramData\HawkHive\DPC8001`。
- 使用 SYSTEM 身份的 Windows 任务计划在开机时运行，不依赖用户登录；异常退出后每分钟重启，最多 255 次；任务本身和采集器全局互斥锁都拒绝重复实例。
- 初次安装遇到长时间断网时，后台进程会持续重试报到；云端确认前每 10 秒轮询一次，管理员无需重新生成安装包。
- 每台电脑生成稳定安装 ID 和独立申请密钥；确认后的云端上传令牌使用 Windows 机器级 DPAPI 加密。安装目录中的登记文件在成功取证后删除，下载目录里的原始包保留，因此还能用于其他电脑。
- 云端必须单独配置并稳定保存 `DCP_ACTIVATION_SECRET`；它不进入下载包，且不能与数据库密码复用。
- 实时数据先写本机 SQLite，再幂等上传；云端失败不会删除待上传记录。本地备份、保留策略和采集器实例也使用互斥保护，避免两个进程同时替换备份文件。

这里采用的是“Windows 任务计划后台代理”，不是 SCM 形态的传统 Windows Service。它已经覆盖无人登录启动、SYSTEM 权限、重复实例抑制和异常重启；如果客户 IT 制度强制要求在 `services.msc` 中管理，需另做签名的原生服务包装器。

## 上线验收

1. 新电脑无 Python：双击 ZIP 内 EXE 可完成安装、报到和开机任务注册，并自动出现在云端待加入列表。
2. 重复使用：同一个 ZIP 在至少两台新电脑安装，两台电脑分别出现待确认记录，获批后获得不同上传凭据。
3. 多网卡：Wi-Fi 上云、网线接设备时，能扫描设备网段且不扫描公网。
4. 多车间：同一 Windows 节点发现两台设备，分别分配到两个车间，配置可正确回传。
5. 重复启动：手动再启动 EXE 或旧采集器时，只允许一个实例持有数据库和设备连接。
6. 断网恢复：本地记录持续增加，恢复后自动补传且云端不重复。
7. 断电重启：无需用户登录即可恢复采集；任务异常退出后能自动重启。
8. 身份边界：未批准电脑不能上传；错误申请密钥不能接管同一机器记录；轮换后旧安装包不能继续申请；已批准电脑仍正常上传。
9. 真实协议：设备必须返回有效 Modbus 数据、固件和 `PCS/28.3L` 单位，不能把“端口开放”当成已发现。

## 仍需厂家或客户确认

### 厂家

1. DPC8001-G 是否只允许一个 Modbus TCP 客户端同时连接；现有协议资料没有说明并发连接上限，需确认厂家上位机与采集器同时运行是否会争用连接。
2. 是否存在可读取且长期稳定的设备序列号/唯一 ID 寄存器。当前协议资料没有足以替代 IP 的稳定身份字段，IP 改变会被视为新候选设备。
3. 客户现场是否可能使用非 `502` 端口或非 `1` Slave ID；若可能，需明确允许的候选范围，不能默认扫描 `1–247` 全地址。

### 客户/客户 IT

1. Windows 支持范围（建议明确 Windows 10/11 64 位）以及是否强制要求代码签名、传统 Windows Service、域策略或指定安装目录。
2. 每台采集电脑所连接的设备 VLAN/网段；若设备跨路由而非直连本机网卡，需要给出允许扫描的 CIDR 白名单。
3. 设备 IP 是否由静态地址或 DHCP 保留保证稳定，以及是否有厂家上位机长期占用同一设备连接。
4. 生产环境允许自动探测的时间窗口、频率和安全审计要求。
5. 采集电脑损坏时，谁有权停用旧节点，以及是否需要双人复核；新电脑直接用通用包重新报到，不再需要覆盖旧机器身份。
