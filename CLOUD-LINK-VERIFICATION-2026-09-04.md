# Windows 与云端联调记录（2026-09-04）

## 已验证

- Windows：192.168.2.53；设备：192.168.2.30:502，Slave 1。
- 云端：https://43.134.164.108；站点 site-001；设备 device-001。
- 已安装的旧服务 HawkHiveCollector 可以获取云端配置，但批量上传返回 HTTP 422，云端没有写入这些批次。旧服务已停止，原数据和安装文件均保留。
- 从本项目已有构建产物启动新版 EXE，未安装 Python。
- Windows 运行目录：`C:\Users\mac\rdpc-cloud-link`；程序 `collector.exe`；数据目录 `live-data`；管理页面 `http://127.0.0.1:8789/collector.html`。
- 启动参数：`collector.exe --device --port 8789 --data-dir live-data`。
- EXE SHA-256 在 Mac 与 Windows 两端核验一致：`d117d0a2bb91c8af786e20757656096c0af6f60e02ac4b6e39b18f4c602111fc`。
- 通过本机接口保存现有站点凭据；没有重新签发凭据，没有放宽云端校验。辅助脚本及传输目录不含云端密钥，密钥仅输入 Windows 隐藏提示。
- 云端配置、心跳、最新数据、历史批次接口均出现来自现场的 HTTP 200。
- 云端历史首条真实记录：2026-09-04 14:41:00.648994 UTC（北京时间 22:41:00），record_uuid `b76eccb5-dd5a-4432-a52b-e68037a43c53`，source=device。
- 粒子单位代码 1，PCS/28.3L；协议档案 dpc8001-g-protocol-2025-06-04。
- 六通道计数：656150 / 199130 / 43720 / 7230 / 1160 / 270。
- 后续最新读数时间持续前进，温湿度变化；Windows 状态 source=device、mode=device、pending_uploads=0、quarantined_uploads=0。
- 已登录云端 Live 页面显示 Online、Real device、上述六通道数据及持续刷新的时间；不是只验证健康接口。
- 临时局域网文件传输容器已停止。

## 尚未通过 / 使用限制

- 当前为前台便携 EXE 联调运行，必须保持采集窗口运行；尚未替换为新版后台 Windows 服务，也未验证重启后的自动恢复。旧服务仅停止，未修改其启动类型，重启前须完成服务切换以避免旧实例抢占站点租约。
- 新版 EXE 的每日备份仍报 WinError 32（临时 SQLite 备份文件被占用）；当前主库文件与上传队列正常工作不能代表备份成功。应修复并重新构建后再做备份恢复验收。
- 设备返回 flow=0，粒子累计值未变化；通信成功不等于设备正在进行新的采样。未发送任何设备启动、停止或参数写入命令。
- 旧版的 830 条待上传记录仍保留；其来源/格式及补传策略待单独核验，不能为了消除 422 而绕过云端验证。
- 本次没有重新构建 EXE，没有部署云端代码，没有完成断网补传、重启恢复、多设备并发或长时间稳定性验收。

结论：Windows → 真实设备读取 → 云端入库 → 已登录页面显示的基本通信链路已打通；这不代表整个采集器已达到正式交付标准。
