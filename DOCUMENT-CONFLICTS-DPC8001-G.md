# DPC8001-G 原始资料互斥项与裁决记录

整理日期：2026-09-03  
适用型号：`DPC8001-G`  
资料范围：微信文件目录中的独立《通讯协议》PDF、设备说明书 PDF、上位机使用指南 DOCX。

## 1. 结论

原始资料中确认有 **7 组会影响程序实现的硬冲突**，另有 **1 项违反常规 Modbus 语义的高风险示例**。

2026-09-03，项目方转达厂家裁决：**DPC8001-G 以独立《通讯协议》为准**。据此，两份 PDF 之间的 C-01～C-05 已完成“采用哪份定义”的裁决；当前实现本来就采用独立《通讯协议》，不需要修改实时寄存器解析逻辑。

这项裁决不能消除独立《通讯协议》自身的 C-06、C-07，也不能自动证明 R-01 的广播读取可以在多设备总线上安全使用。

| 项目 | 状态 | 处理 |
|---|---|---|
| C-01～C-05 | 已裁决 | 以独立《通讯协议》定义为准；仍需真实设备核值 |
| C-06、C-07 | 未关闭 | 通讯协议内部自相矛盾，继续禁止相关参数写入 |
| R-01 | 未关闭 | 不使用地址 `0` 广播读，RS485 按 `1–247` 轮询 |

项目继续保持以下策略：

- 实时只读字段按独立《通讯协议》实现，但仍需真实设备验证数值、单位和固件适配。
- 所有存在冲突的配置、启停、工作模式、定时、单位和报警寄存器不在客户页面或公开 API 中开放写入。
- 扫描仅把设备列为候选；必须同时验证目标协议和粒子单位，且仍需人工确认设备归属。

## 2. 硬冲突：同一寄存器被定义为不同数据

### C-01 环境参数 `12–18` 完全错位

| 地址 | 独立《通讯协议》PDF 第 2 页 | 设备说明书 PDF 第 19 页（页脚 14） |
|---:|---|---|
| `12–13` | 流量值，float | 温度值，float |
| `14–15` | 温度值，float | 湿度值，float |
| `16–17` | 湿度值，float | 露点值，float |
| `18–19` | 露点值，float | 流量值，float |

裁决：采用独立《通讯协议》的顺序。当前代码与裁决一致，但仍要用真实设备屏幕与原始寄存器逐项核对。

### C-02 `24–27` 的数据类型和业务含义互斥

| 地址 | 独立《通讯协议》PDF 第 3 页 | 设备说明书 PDF 第 19 页（页脚 14） |
|---:|---|---|
| `24` | 粒子报警位，u16，占 1 个寄存器 | 电池电压，float，占 `24–25` 两个寄存器 |
| `25` | 洁净等级，u16 | 电池电压的第二个字 |
| `26` | 未在该表定义 | 洁净等级，u16 |
| `27` | 未在该表定义 | 故障状态，u16 |

裁决：采用独立《通讯协议》的 `24=报警位`、`25=洁净等级`；说明书中的电池电压、故障状态定义不用于 DPC8001-G 采集器。真实设备核值前仍不能宣称现场验收完成。

### C-03 `34–35` 是循环次数还是采样用时

- 独立《通讯协议》PDF 第 3 页：`34` 为“循环次数”，u32，连续模式下递增。
- 设备说明书 PDF 第 19 页（页脚 14）：`34` 为“采样用时”，u32。

裁决：`34–35` 按独立《通讯协议》解释为循环次数。当前程序不读取该字段；后续若用于新样本判断，必须先做真实设备验证。

### C-04 `124` 是报警通道使能还是显示通道设置

- 独立《通讯协议》PDF 第 5 页：`124` 为“报警通道使能设置”，BIT0–BIT5 对应六个粒径报警；`134` 才是粒子、流量、温湿度等显示通道使能。
- 设备说明书 PDF 第 20 页（页脚 15）：`124` 为“通道显示设置”，没有列出 `134`。

裁决：采用独立《通讯协议》，`124` 为报警通道使能、`134` 为显示通道使能。当前客户产品仍不写这两个寄存器，报警策略由软件侧管理。

### C-05 定时寄存器 `126–131` 定义不一致

| 地址 | 独立《通讯协议》PDF 第 5 页 | 设备说明书 PDF 第 20 页（页脚 15） |
|---:|---|---|
| `126` | 启动延时 | 连续模式启动延时 |
| `127` | 采样时间 | 间歇模式启动延时 |
| `128` | 间歇时间 | 间歇模式工作持续时间 |
| `129` | 采样周期/采样次数，范围 `1–9999` | 间歇模式工作停止持续时间 |
| `130` | 未定义 | 单次模式启动延时 |
| `131` | 未定义 | 单次模式工作持续时间 |

裁决：采用独立《通讯协议》对 `126–129` 的定义，`130–131` 不使用。由于工作模式和默认值在通讯协议内部仍有矛盾，遗留的 `129` 写操作保持删除，当前产品不写这些寄存器。

## 3. 同一份《通讯协议》内部自相矛盾

### C-06 工作模式 `125` 的合法值互斥

- 独立《通讯协议》PDF 第 5 页表格：`0=连续模式`、`1=自动模式`，默认值 `1`。
- 同一文件第 12 页示例：值 `1` 被称为“间歇工作模式”，值 `2` 被称为“单次工作模式”。

问题：表格根本没有定义值 `2`，并且“自动模式”和“间歇模式”是否同义也没有说明。当前程序不写工作模式。

### C-07 启动延时出厂默认值是 10 秒还是 60 秒

- 独立《通讯协议》PDF 第 1 页正文：出厂默认启动延时 `10s`，采样 `60s`，间隔 `60s`。
- 同一文件第 5 页寄存器表：`126` 启动延时默认值为 `60`。

影响：安装验收、启动等待和超时策略无法只按一个默认值设计。程序应读取状态并容忍至少 6 秒的上电不响应，不应主动写入所谓默认值。

## 4. 高风险示例：并非两份文件互斥，但不应直接实现

### R-01 使用广播地址 `0` 读取设备地址

独立《通讯协议》PDF 第 12 页给出：

- 正常读取：从站 `01`，读取地址 `148`。
- 所谓广播读取：从站 `00`，同样使用功能码 `03` 并期待设备返回。

常规 Modbus 广播请求不应获得从站响应；同一总线上若多台设备同时响应还可能发生冲突。因此，除非厂家提供专用发现机制、响应时序和多设备碰撞处理说明，采集器不得使用该示例做自动扫描。当前 RS485 地址扫描应按 `1–247` 逐地址、受控轮询。

## 5. 容易误判、但目前不属于互斥的内容

以下差异需要说明，但不能与硬冲突混在一起：

1. **`2.83 L/min` 与 `PCS/28.3L` 不冲突。** 前者是设备物理采样流量（设备说明书 PDF 第 6、7 页），后者是粒子计数的显示/归一化单位（独立协议 PDF 第 5 页）。两者量纲和用途不同。
2. **主界面“颗/m³”与四种可选单位暂属歧义。** 设备说明书 PDF 第 12 页把实时六通道浓度写为“颗/m³”，但第 15 页只说有四种单位；独立协议给出寄存器 `133` 的四个代码。只有在厂家声明主界面固定为 m³、协议默认又固定为 28.3L 时，才构成直接冲突。当前程序每周期读取 `133`，不靠界面文案猜单位。
3. **上位机的 `0#` 不是 Modbus 从站地址 0。** DOCX 渲染第 13 页明确将 `0#` 用作“未单独配置设备的默认报警策略”；设备地址仍为 `1–247`。这是软件策略编号，不是广播地址。
4. **RS485 地址扫描与 LAN IP 扫描不是互斥。** DOCX 渲染第 5、9、14 页描述的是同一总线的从站地址 `1–247`；本项目的 `/24` 扫描是寻找 Modbus TCP IP，两者应分别实现和验收。
5. **软件保存间隔与设备采样周期不是同一个参数。** DOCX 渲染第 15 页的 `1–100 秒/分钟` 是上位机数据保存频率；设备寄存器 `126–131` 控制硬件采样阶段。不能把一个直接写入另一个。
6. **DOCX 的功能范围不等于通讯寄存器定义。** 用户、Logo、Excel、历史删除、报警颜色和软件授权属于上位机产品功能，不应反向决定设备寄存器含义。

## 6. 尚未关闭的协议内部问题

“以通讯协议为准”已经解决两份资料之间的优先级，但下面三项仍需厂家补充或通过真实设备验证：

- `125` 的合法值究竟是 `0/1` 还是 `0/1/2`，“自动”和“间歇”是否同义。
- `126` 的出厂启动延时究竟是 `10s` 还是 `60s`。
- 地址 `0` 的广播读取是否为厂家私有扩展；多台设备同时在线时如何防止响应碰撞。

此外仍建议厂家给出本次裁决适用的固件版本范围和通讯协议版本号。否则未来固件升级后，无法判断是否仍适用同一寄存器表。

## 7. 开发放行规则

- C-01～C-05 已完成文档裁决：允许按独立《通讯协议》继续开发；真实设备验证通过前仍不能完成数据正确性验收。
- C-06、C-07 未关闭：可以继续只读采集，但不能下发工作模式或采样定时参数。
- 设备内部报警/显示配置暂不开放给客户；若未来开放，需要单独做写入权限、回滚和真实设备验收。
- R-01 未关闭：不能使用从站地址 `0` 做 RS485 自动发现。
- 所有冲突关闭后，仍需在真实 DPC8001-G 上逐项核对“设备屏幕 → 原始寄存器 → 本地数据库 → 云端数据库 → 页面”。
