# DPC8001-G 文档追溯与缺口审计

审计日期：2026-09-04  
审计范围：项目内全部 Markdown 文档、厂家两份 PDF、厂家上位机使用指南 DOCX，以及采集、存储、同步、云端接口和 Windows 服务源码。

型号范围：用户已确认目标型号为 `DPC8001-G`。说明书标题中同时出现的 `S`、`X-G`、`X-S` 仅作为资料背景，不属于当前适配器已支持型号。

## 1. 审计结论

当前源码已经具备“只读采集 → 本机先落盘 → 可靠补传 → 云端查询”的试点基础，但仍不能判定为量产可交付，主要原因不是代码数量不足，而是三类证据尚未闭环：

1. 厂家已确定 DPC8001-G 在资料冲突时以独立《通讯协议》为准；但该协议内部的工作模式枚举和启动延时默认值仍自相矛盾，且尚未记录裁决适用的固件范围。
2. 厂家资料没有给出可读取的型号、序列号或其他强设备身份，自动扫描只能形成候选，不能自动信任和纳管。
3. 当前已完成 macOS 源码检查、自动测试、Windows 11 单文件 EXE 基线验证和真实设备只读采集；修复后的 EXE、Windows 服务、正式云端和长时间断网补传仍需分别验收。

因此，跨文档寄存器优先级不再阻塞开发，试点可以继续；但仍要保持以下安全边界：只读；人工确认业务归属；仅接受已确认的粒子单位；模拟数据不上云；不得把本地测试称为现场验收。

原始资料的完整硬冲突、非冲突差异和厂家裁决模板，见 `DOCUMENT-CONFLICTS-DPC8001-G.md`。

## 2. 输入资料清单、哈希与裁决规则

项目目录中现有产品文档均为 Markdown；审计时没有发现 PDF、DOC 或 DOCX 原件。以下厂家原件位于本机微信文件目录，尚未纳入项目或白名单发布包：

| 资料 | 页数 | SHA-256 | 用途与状态 |
|---|---:|---|---|
| `通讯协议(1)(1).pdf` | 13 | `c6180f6b6bab7a244c1bd8c0bc8a598da294e9ba6f83f4afbcfac18e10860529` | 2025-06-04 版本；厂家已确定 DPC8001-G 以此为准，仍需补充适用固件范围 |
| `FK-DPC8001-G／S、FK-DPC8001X-G／S 壁挂式尘埃粒子计数器说明书-CN1.pdf` | 21 | `bdfde67773b0113ab4d6944aa9b0c2660501d108e37ec6fe3cf419e0092bf145` | 设备说明、操作、维护和另一套寄存器表；与独立协议有冲突 |
| `MY洁净室环境在线监测系统 上位机使用指南.docx` | 20（本机渲染） | `766bce3cca29f5c6aec3a29d493cf4342bfdabeb11dd044807d0ed1ba5a47614` | 说明厂家上位机的 RS485/TCP、地址扫描、保存、报警和用户权限能力；不是本项目需求或协议的替代品 |

注意：哈希只能证明本次审计所读文件版本，不能证明厂家已正式批准。建议取得有版本号、发布日期和变更记录的正式文件，再复制到受控的 `docs/vendor/`，并确认授权范围后决定是否进入交付包。

三份资料同时作为输入基线，但不能在互相冲突时同时直接控制代码。项目采用以下裁决顺序：

1. 根据 2026-09-03 项目方转达的厂家裁决，DPC8001-G 以独立《通讯协议》控制线上的功能码、寄存器地址、字序和报文格式。
2. 设备说明书控制型号差异、电气/安装、标称性能、设备端操作和维护要求。
3. 《上位机使用指南》控制最低用户流程、角色、查询、导出、报警配置和连接方式的需求对照，不直接替代通讯协议。
4. 两份资料互斥时按独立《通讯协议》处理；独立《通讯协议》内部仍有冲突时，代码保持只读和失败关闭，不靠推测补全定义。

设备说明书仍控制安装、操作和维护，但其中与通讯协议互斥的寄存器表不用于 DPC8001-G 采集器。若厂家之后发布正式勘误或新版协议，以新的版本化决议覆盖本表，并保留变更记录。

## 3. 关键事实与冲突矩阵

| 主题 | 独立《通讯协议》 | 设备说明书 | 当前处理 | 风险/下一证据 |
|---|---|---|---|---|
| 实时环境寄存器 | `12/14/16/18` 为流量/温度/湿度/露点 | `12/14/16/18` 为温度/湿度/露点/流量 | 已裁决并采用独立协议 | 文档冲突关闭；真实固件仍需逐项对照屏幕和原始寄存器 |
| `24–27` 含义 | `24` 为报警位，`25` 为洁净度 | `24` 起为电池电压 float，`26` 洁净度，`27` 故障 | 已裁决并采用独立协议的 `24/25` | 文档冲突关闭；真实报警和洁净度仍需核值 |
| `34–35` 含义 | 循环次数，u32 | 采样用时，u32 | 已裁决为循环次数；当前不读取 | 若未来用于新样本判断，先做真实设备验证 |
| `124/134` 含义 | `124` 报警通道使能；`134` 显示通道使能 | `124` 通道显示设置；未列 `134` | 已裁决采用独立协议；当前不写入 | 未来开放写入前另做权限、回滚和设备验收 |
| `126–131` 定时参数 | `126` 启动延时、`127` 采样时间、`128` 间歇时间、`129` 采样周期；未定义 `130/131` | 按连续、间歇、单次模式分别定义六个时间参数 | 已裁决采用独立协议；当前不读取和写入 | 协议自身模式/默认值仍矛盾，暂不下发采样时序 |
| 工作模式 `125` | 表格仅定义 `0=连续`、`1=自动`，示例又出现 `1=间歇`、`2=单次` | 定义连续、间歇、单次模式 | 当前不写入 | P0：需确认合法值、状态转换及非法值处理 |
| 启动延时默认值 | 独立协议正文写 `10s`，寄存器表写 `60` | 未能消除该矛盾 | 不主动写默认值；容忍上电至少 6 秒无响应 | P0：安装验收和超时不能依赖单一默认值 |
| 粒子单位 | `133`：0=`PCS/L`、1=`PCS/28.3L`、2=`PCS/m³`、3=`PCS/2.83L`，默认 1 | 主界面文字写“颗/m³” | 每个采集周期先读 `133`；只接受 1，其他值明确失败；单位和协议档案随记录保存 | P0：厂家确认单位寄存器是否改变原始值还是仅改变显示 |
| 粒子采样语义 | 提供寄存器和定时参数，没有说明新样本序号 | 说明连续/间歇模式，默认测量 60 秒、停止 60 秒 | 当前按轮询时间保存；尚不能识别停采阶段的旧值保持 | P0：需厂家提供采样状态/序号或冻结判定依据 |
| 设备身份 | 未提供序列号、型号、MAC 读取字段 | 屏幕可能显示设备信息，但未给协议地址 | 扫描只验证目标寄存器可读，纳管需人工确认 | P0：需唯一身份寄存器或客户资产台账与人工绑定规则 |
| 外接传感器 | 提供温湿度、露点、风速、压差字段 | 多项为外接/选配 | 当前读取字段但没有能力声明 | P0：未安装项不得当作 0 或参与报警 |
| 自动扫描 | 未定义广播、mDNS 或发现报文 | 无 LAN 自动发现定义 | 本项目受控扫描私有 IPv4 `/24` 和 TCP 502；真实 `/24` 扫描已发现并协议验证 `.30` | 这是采集器主动逐 IP 验证，不是设备原生发现；厂家上位机的 1–247 是从站地址扫描 |
| 维护 | 未提供维护状态寄存器 | 要求每月清洁采样口、每年校准；气泵寿命大于 8000 小时 | 尚无维护台账功能 | 客户需指定责任人、证书留存和逾期处理规则 |

## 4. 厂家上位机流程逐项对照

| 厂家指南能力 | 当前产品状态 | 结论/处理 |
|---|---|---|
| 登录、管理员与普通用户、多角色 | 已有登录与管理员/只读角色基础；无客户自助用户管理页面 | P1 缺口；客户确认用户数、角色和密码/MFA策略 |
| 串口与 TCP 两种连接 | 采集库有 RTU 诊断客户端；后台服务和自动发现当前只接入 Modbus TCP | 范围缺口；客户确认现场是否要求 RS485 直连，若要求则进入 P0 |
| 扫描 RS485 地址 `1–247`、禁止重复地址 | 当前扫描局域网 IPv4/TCP，设备登记校验端点与 Slave ID 重复 | 两种扫描不是一回事；RS485 总线扫描尚未接入服务 |
| 自动扫描与手动添加设备 | 已有 LAN 自动候选扫描、连接测试、人工命名归属和手动登记 | 已有基础；新增候选必须同时通过目标协议和单位门禁 |
| 串口、波特率、连接设置持久化 | TCP 端点持久化；后台服务无串口配置 | 随 RS485 范围决定 |
| 设备在线/离线与最后数据 | 已有设备状态和时间 | 仍需真实离线、采集器离线、云端离线三类现场验收 |
| 参数值、单位、上下限颜色 | 已显示值、单位和报警状态；粒子仅上限，温湿度为上下限 | 不机械复制黄色/绿色/红色；需客户确认业务阈值和无障碍表达 |
| “0# 默认策略 + 单设备覆盖” | 当前以洁净室/房间为阈值作用域 | 明确功能缺口；建议实现“房间默认 + 单设备覆盖 + 回退默认” |
| 报警查询 | 已有只读报警历史 | 处置确认、备注、责任人和恢复原因尚未实现 |
| 历史按全部/今天/时间/地址查询、分页 | 已支持时间、房间/设备筛选和历史分页/采样 | 需补真实大数据量和站点时区验收 |
| Excel 导出 | 已有 XLSX/CSV 基础 | 导出仍缺站点时间、质量标记、完整单位/协议字段，不能作为合规报表验收 |
| 手工删除 30/60/90/120 天历史 | 当前不向业务用户开放直接删除，采用保留/归档策略 | 有意的安全差异；保留期限和删除授权需客户签字确认 |
| 实时/历史曲线及通道显隐 | 已有趋势图和多通道选择 | 需真实设备、多设备和手机端用户路径验收 |
| 设备新增、修改、删除；只改工作地址 | 已有新增、连接参数修改和软停用 | 采用软停用保历史，优于直接删除；编辑/迁移的权限审计仍不完整 |
| 保存周期 1–100 秒/分钟 | 采集轮询与永久记录间隔可由环境配置 | 未形成管理员可控且有安全上下限的产品配置；需与设备采样周期对齐 |
| 自定义 Logo/文字、仪表盘行列 | 尚未实现 | P2 白标和布局能力，除非客户列为验收项 |
| 10 天试用、机器码、厂家授权 | 尚未实现 | 商业授权方案不能从指南自动继承；由本项目商务/客户另行决定 |

### 本轮新发现的代码级缺口

- 演示流量曾以 `28.3 L/min` 生成，与设备说明书标称 `2.83 L/min` 相差十倍；已修正并增加回归测试。
- 扫描曾只读取实时值和固件寄存器，未验证粒子单位；现将“Modbus 响应”“目标协议响应”“单位支持”“可安全添加”拆成独立证据，非 `PCS/28.3L` 候选不能勾选纳管。
- 云端扫描结果原来只保存一个 `verified` 布尔值，无法解释失败原因；现保存协议、固件、单位和身份验证证据。
- 历史归档原来遗漏粒子单位和协议档案；现把三个字段加入归档并在 manifest 中固定列定义。
- 源码中存在未路由的设备启停/采样设置处理函数，且曾写入存在冲突的寄存器 `129`；已删除。诊断 CLI 的启停写入现在要求额外显式授权参数，客户页面和公开 API 仍无设备写入口。

## 5. 需求到实现与证据

| 需求 | 源码落点 | 自动化证据 | 当前状态 |
|---|---|---|---|
| Modbus TCP/RTU 只读采集 | `dcp8001_collector.py` | `test_dcp8001_collector.py` | 标准 Modbus TCP 已用 `.30` 真机复验；RTU 仅有诊断单测，无真实串口证据 |
| 多设备并发与离线退避 | `monitoring_service.py` | `test_monitoring.py` | 本地通过；无 10/30/50 台负载基线 |
| 多网卡候选与受控 `/24` 发现 | `device_discovery.py`、本机维护页 | `test_monitoring.py`、`test_security_boundaries.py` | Windows 活动 WLAN 网段识别已验证；修复后 `.30` 的完整 `/24` 扫描在当前局域网通过，修复版 EXE 待复验 |
| 先落盘后上传、UUID 幂等、隔离坏数据 | `dashboard_server.py`、`cloud_sync.py`、`cloud_api.py` | `test_monitoring.py` | `.30` 真机读数已写入隔离 SQLite 并保持待上传；无真实 24 小时断网及恢复补传验收 |
| 单位和协议档案全链路保存 | 采集器、本地 SQLite、同步载荷、PostgreSQL schema/API、页面 | 协议单测及全量回归 | 本轮补齐；旧记录会显示“单位未确认” |
| 本机保留、磁盘水位、每日备份 | `storage_maintenance.py` | `test_storage_maintenance.py` | 本地通过；无恢复演练和独立备份介质验证 |
| Windows 无人值守 | `windows_service.py`、安装/卸载 PowerShell | `test_runtime_foundation.py` | Windows 11 单文件 EXE 无 Python启动已验证；服务安装、重启和卸载仍未验证 |
| 凭据保护与本机边界 | `collector_settings.py`、服务安装 ACL、回环监听与写请求保护 | `test_collector_settings.py`、`test_security_boundaries.py` | 基础实现；未验证域策略、EDR、普通用户 ACL 和本机多用户威胁 |
| 云端 HTTPS、租户隔离、登录和报表 | `cloud_api.py`、Caddy、PostgreSQL、前端 | `test_monitoring.py`、`test_security_boundaries.py` | 本地/容器基础；未部署到正式域名，也没有真实账号用户路径验收 |
| 安全发布 | `build_release.py` 白名单打包 | `test_release_package.py` | 可生成无运行数据的源码包；没有签名 EXE/MSI 与升级回滚 |

## 6. 本轮已补齐的缺口

- 新增粒子单位寄存器校验；不再把未知单位静默标成 `particles/ft³`。
- 将单位代码、单位标签和协议档案贯穿本地库、上传队列、云端数据库、最新值、历史和页面。
- 页面按记录显示单位；旧数据缺少单位信息时显示“单位未确认”，多设备数据单位不一致时显示“单位不一致”，模拟数据继续保留醒目的来源警告。
- 记录厂家“DPC8001-G 以通讯协议为准”的裁决；五组跨文档冲突关闭，两组协议内部冲突和广播读取风险继续跟踪。
- 修正文档中“已上线”“缺少 Windows Service/备份/DPAPI”等与当前源码事实不一致的表述。
- 容器启动验证发现并修复 `Dockerfile.cloud` 漏拷贝 `alarm_config.py` 的发布缺陷；仅“镜像构建成功”不足以证明服务可启动。
- 三条云端写入通道现在都会在服务端确认“启用设备属于令牌客户、站点和房间”，并使用数据库中的规范名称；报警 UUID 与其他目标冲突时拒绝覆盖。
- 扫描结果只有在实时寄存器、固件寄存器与粒子单位都验证通过后才标记为可添加；云端保留各层证据而非只留一个结论值。
- 修正演示流量十倍偏差、归档元数据遗漏和遗留写寄存器入口。
- 真机证明 TCP `502` 使用标准 MBAP 而非 RTU 透明传输；修复 Wi-Fi 模块遗留旧事务响应和 U32 半字段探测问题，真实 `/24` 扫描与协议门禁已通过。
- 真机读数已通过后台服务进入隔离 SQLite 并保持未同步状态，证明“只读采集到本机待传”的最短闭环；测试数据已清理。
- 真机长连接按 10 秒默认轮询完成 12/12 次短时采集，未出现错误；这支持连接复用方案，但不等同于多设备或 24 小时稳定性验收。

## 7. 仍然阻塞量产的 P0

1. 厂家补充通讯协议适用的固件范围，澄清 `125` 合法模式、`126` 默认值和地址 `0` 广播读取规则，并提供强设备身份或明确表示不支持。
2. 客户提交设备台账、网络/IP 规划、目标 Windows 版本、容量/留存、报警责任和正式验收场景。
3. 当前已闭环“原始寄存器 → 本地 SQLite”；仍需在设备启动采样后对照“仪表屏幕 → 原始寄存器 → 本地 SQLite → 云 PostgreSQL → 远程页面”的同一时间、值和单位。
4. 在目标 Windows 上验证安装、无人登录启动、退出恢复、断电、ACL、DPAPI、卸载保留数据和 EDR 兼容性。
5. 进行真实断网补传、磁盘不足、数据库损坏、云端 429/5xx/422、错误配置和令牌吊销演练。
6. 制作并验证签名安装包、升级包、哈希校验和失败回滚，之后才可称为量产发布。

## 8. 判定规则

- “源码存在”不等于“目标系统可运行”；必须另有 Windows 实机证据。
- “测试通过”不等于“真实协议正确”；协议正确性必须由正式资料和硬件对照共同证明。
- “页面有数值”不等于“链路完整”；至少要核对设备、本地、云端和页面四段的 ID、时间、值、单位与来源。
- “端口 502 可读”不等于“设备身份可信”；没有强身份时必须保留人工确认。
- 这些规则适用于网络不稳定、协议可变的工业边缘采集；反例是设备具有受信证书、签名遥测和厂家提供的版本化自描述协议，此时可以适度提高自动信任程度。
