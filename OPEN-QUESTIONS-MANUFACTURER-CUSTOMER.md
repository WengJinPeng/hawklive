# Windows 采集上传器外部确认清单

版本：2026-09-04  
用途：以下问题只保留无法通过现有说明书、通讯协议或源码可靠推导的外部事实。建议要求对方以邮件、盖章文档或协议修订版书面回复，避免口头答复成为后续验收争议。

已确认范围：目标型号为 `DPC8001-G`；`S`、`X-G`、`X-S` 暂不进入本期支持范围。

## 一、已从资料确认，不需要重复询问

- 设备支持 RS485 与 RJ45；寄存器 `151` 把网络端口标为 Modbus TCP 端口，默认值为 `502`。
- 独立《通讯协议》的总线说明和示例实际采用 RS485 / Modbus RTU CRC 帧；资料没有提供标准 Modbus TCP（MBAP）或透明 RTU-over-TCP 的成功请求/响应示例。
- 2026-09-04 已用真机只读报文确认 `192.168.2.30:502` 使用标准 Modbus TCP（MBAP），Unit ID 为 `1`；将 RTU CRC 帧发送到该端口会返回 MBAP 异常，因此当前设备不是透明 RTU-over-TCP。
- 独立《通讯协议》规定实时数据地址为：流量 `12`、温度 `14`、湿度 `16`、露点 `18`、风速 `20`、压差 `22`、报警 `24`、洁净度 `25`、固件版本 `36`。
- 协议公开了读取保持寄存器 `0x03`、写单寄存器 `0x06`、写多寄存器 `0x10`。
- 协议中的默认局域网地址为 `192.168.1.88`，Modbus TCP 端口配置寄存器为 `151`。
- 设备说明书称上电后约 6 秒再响应，出厂间歇采样为运行 60 秒、停止 60 秒。
- 厂家上位机的“扫描”说明主要是扫描 RS485 从站地址 `1–247`；这不能证明它支持跨多网卡自动扫描所有局域网 IP。
- 2026-09-03，项目方转达厂家确认：DPC8001-G 在说明书与独立《通讯协议》冲突时，以独立《通讯协议》为准。寄存器 `12–27`、`34–35`、`124/134`、`126–131` 的跨文档优先级不再重复询问；仍需真实设备验证。

## 二、请厂家确认（其中 P0 会阻塞设备真实性与数据正确性验收）

### P0：必须在现场联调前答复

1. **通讯协议自身的矛盾和危险示例如何解释？**
   - 工作模式寄存器表写 `0=连续、1=自动`，示例又出现 `1=间歇、2=单次`；请给出全部合法值、状态转换和非法值处理。
   - 同一协议正文写启动延时默认 `10s`，寄存器表写默认 `60`；请确认最终出厂默认值。
   - 请确认从站地址 `0` 的“广播读设备地址”是否为厂家私有扩展，并提供多设备同时在线时的防碰撞规则；否则请正式作废该示例。
   - 请补充“独立《通讯协议》版本 → DPC8001-G 固件版本”的适用关系。

2. **是否有可稳定识别设备身份的字段？**
   - 请提供型号、产品序列号、MAC、设备唯一编号、硬件版本的读取地址与编码方式。
   - 是否支持 Modbus Device Identification（功能码 `0x2B/0x0E`）？若不支持，请明确回复“不支持”。
   - 当前资料没有身份寄存器，因此采集器最多只能确认“这是一个能响应已知寄存器的 Modbus 设备”，不能安全宣称“已验证为 DPC8001”。

3. **粒子计数值的单位语义是什么？**
   - 单位设置寄存器 `133` 是否会改变实时寄存器返回值，还是只改变屏幕显示？
   - `PCS/L`、`PCS/28.3L`、`PCS/m³`、`PCS/2.83L` 的换算、取整和溢出规则是什么？
   - 6 个粒径通道是瞬时值、一次采样累计值，还是滚动周期值？采样周期从哪个寄存器读取？
   - 当前采集器为避免单位误标，只接受寄存器 `133 = 1`（`PCS/28.3L`）；请确认这是否能作为现场准入条件。

4. **以太网连接限制是什么？**
   - 每台设备最多允许多少个并发 TCP 客户端、连接空闲多久关闭、是否支持长连接？
   - 官方建议的最短轮询间隔、响应超时和失败重试间隔是多少？
   - 同一 IP 下是否可能通过不同 Unit ID 接入多台设备，还是“一台设备一个 IP，Unit ID 固定为 1”？
   - 真机出现“新连接收到上一连接的迟到响应”，且快速连续建立短连接会间歇超时；请确认网口模块的缓存、连接释放和最大客户端规则。

5. **无效数据与停采状态如何表示？**
   - 传感器未安装、探头故障、气泵停止、间歇采样的停止阶段分别返回什么值？例如 `0`、`0xFFFF`、保持上次值或异常码。
   - 报警寄存器 `24` 每一位的正式定义是什么？数据在设备报警时是否仍可作为有效测量值上传？
   - 停采阶段是否保持最后一次读数？若是，是否有样本序号、采样状态或完成标志，可用于区分“新样本”和“旧值保持”？

6. **固件版本寄存器 `36` 的编码格式是什么？**
   - 请给出至少两个实际固件版本及对应原始寄存器值，用于编写自动兼容和回归测试。
   - 当前真机寄存器 `36` 返回 `0`，与协议默认值 `100` 不一致；请确认 `0` 的含义以及当前设备固件版本。

### P1：试点期间答复

7. 网络参数 `236` 起始的一组寄存器是否允许通过 TCP/RTU 写入？写入后是否要重启？写错网关或掩码时如何恢复出厂网络配置？
8. 设备是否支持 DHCP、mDNS、广播发现或厂商发现报文？如支持，请提供报文格式；如不支持，采集器只能使用受控网段探测。
9. 流量、温湿度、露点、风速、压差的量程、精度、无效值和小数缩放是否随外接传感器型号变化？
10. 设备时间能否读取/校准？实时值是否自带采样时间，还是应以 Windows 采集时间为准？
11. 固件升级是否可远程执行？升级失败的恢复方式、签名校验和兼容性策略是什么？本项目第一期不会开放远程写寄存器或固件升级。
12. 校准证书、校准周期、气泵寿命告警和维护状态能否通过协议读取？

## 三、请客户确认（P0 会影响部署架构、容量和验收）

### P0：立项/试点前必须答复

1. **设备清单**：每个车间的 DPC8001-G 数量、固件版本、序列号、当前 IP、TCP 端口、Unit ID、安装位置；预计一年内最大扩容数量；另列任何非 DPC8001-G 设备。
2. **网络拓扑**：每台 Windows 电脑连接哪些网卡、IP/掩码/VLAN；设备是否跨网段；是否存在 VPN、虚拟网卡、双网关、代理或 802.1X；能否允许 TCP `502` 和 HTTPS `443`。
3. **IP 管理方式**：设备使用静态 IP 还是 DHCP 保留地址；由谁分配与维护；如何避免不同设备使用相同的出厂 IP `192.168.1.88`。
4. **Windows 环境**：Windows 10/11/Server 的具体版本；是否有本机管理员权限；电脑是否专用；是否允许安装 Windows 服务；是否会休眠/关机；是否有 UPS、杀毒/EDR 和域策略限制。
5. **采集与留存**：期望采集频率、云端展示延迟、最长断网时长、本机至少保留多少天、云端保留多少年。若未定，试点建议先按 60 秒采集、7 天离线容量、90 天本机已上传数据设计。
6. **云端出口**：使用的正式域名、服务器区域、TLS 证书签发方式、是否走 HTTP 代理、是否有企业自签 CA/出站白名单、数据是否允许离开所在国家或地区。
7. **组织结构**：公司/厂区/车间/房间/设备的正式命名与唯一编号；办公室、值班、管理员分别可看哪些厂区，谁能修改设备与报警配置。
8. **报警责任**：报警阈值依据什么标准，通知给谁，通过短信/电话/企业微信/邮件中的哪一种；夜间升级路径、确认时限和误报处理人是谁。
9. **验收场景**：试点车间、上线日期、现场联系人；需要验证的断网时长、断电恢复、设备掉线、重复数据、跨日/跨时区报表和并发查看人数。
10. **粒子单位策略**：试点是否可以统一锁定设备寄存器 `133 = 1`（`PCS/28.3L`），并以此制定报警阈值；若不能，必须先确定多单位换算和阈值管理规则。
11. **厂家上位机能力取舍**：本期是否必须同时支持 Modbus TCP 与 RS485 串口直连？报警是否需要“房间默认 + 单设备覆盖”？自定义 Logo/布局、客户自助用户管理、历史手工删除和软件授权分别是否属于验收范围？

### P1：试点范围确定后答复

12. 是否需要导入历史数据；报表字段、格式、语言、时区和导出模板是什么？
13. 是否要求审计日志保存期限、账号 MFA、密码策略、单点登录或客户自有身份系统对接？
14. 采集器软件由谁升级、允许的维护窗口、失败回滚时限、远程支持方式是什么？远程支持是否允许访问设备局域网？
15. 本机数据盘容量与磁盘类型；磁盘空间不足时，客户倾向于“停止采集并告警”还是“只清理已成功上传的最旧数据”。项目默认不会删除未上传数据。
16. 是否需要高可用：一台车间电脑故障时是否允许暂时无采集，还是要求两台采集器主备；若主备，设备是否允许两个客户端同时读取。
17. 对设备状态的业务定义：多久无数据算离线、多久恢复算在线、维护中的设备是否参与报警和可用率统计。
18. 谁负责每月清洁采样口、年度校准、证书归档和气泵寿命跟踪；逾期时是仅提醒，还是禁止将数据用于合规报告？

## 四、建议客户/厂家提供的最小联调材料

- 一台与量产固件一致的真实设备，或厂家可远程访问的专用测试设备。
- 一份盖版本号和发布日期的最终通讯协议，以及协议变更记录。
- 至少 30 分钟真实原始寄存器抓包/导出，覆盖正常、停采、一个传感器异常和一个设备报警场景。
- 客户试点网络拓扑图、IP 规划表和一台拟部署的 Windows 电脑。
- 云端测试环境的域名、站点编号与专用测试令牌；不得使用生产令牌做开发联调。

## 五、答复验收方式

- 厂家“以通讯协议为准”的回复已经关闭跨文档优先级问题；协议内部矛盾、单位语义和固件适用范围仍必须落到“型号 + 固件版本 + 原始寄存器值 + 工程单位”。
- 客户回复必须能落到具体车间、具体电脑和数量；“都可以”“以后再定”只能作为试点假设，不能作为量产验收依据。
- 所有未答 P0 项进入风险台账；软件可以继续开发通用能力，但对应功能不得标注为已完成生产验收。
