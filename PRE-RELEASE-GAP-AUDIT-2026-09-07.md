# DPC8001-G 自动采集器发布前查缺补漏

日期：2026-09-07  
目标版本：0.5.0  
结论：源码已达到下一轮 Windows 构建与实机验收条件，但尚未达到生产发布条件。

## 本轮已补齐

1. **默认接入改为可复用安装包**：同一客户 ZIP 可用于多台电脑，不再预先建节点或逐台生成一次性包。每台电脑启动后自动进入云端待加入列表，管理员只决定加入或拒绝，并可按需修改名称；无需抄录配对码。
2. **登记权和上传权分离**：安装包只允许创建本客户待确认申请；管理员批准后，云端才为该机器签发独立上传凭据。错误的机器申请密钥不能接管已有登记，通用包泄露时可轮换并吊销全部待确认申请。
3. **激活密钥独立管理**：云端要求配置稳定且不与数据库密码复用的 `DCP_ACTIVATION_SECRET`。它用于派生客户登记令牌和逐机凭据，不进入源码或日志。
4. **发布 EXE 防错发**：云端镜像会包含 `release/`；只有 EXE 存在、具有 `MZ` 头、大小受限且 SHA-256 sidecar 完全匹配时才生成客户包。
5. **扫描任务并发安全**：旧数据库若存在同名普通索引，迁移会先结束重复活动任务，再替换为唯一部分索引；并发请求通过 `ON CONFLICT` 复用已有任务。
6. **扫描历史有界**：已完成或失败的扫描任务和级联结果默认保留 7 天，避免每 5 分钟自动扫描导致 PostgreSQL 无限增长。设备配置、读数和审计记录不受影响。
7. **升级时 EXE 文件占用**：停止旧任务后使用暂存文件与限时重试替换 EXE，避免 Windows 尚未释放旧程序导致直接报 `WinError 32`。
8. **安装结果可验证**：待批准时安装窗口明确提示已自动提交云端；批准后后台自动取证并启动，无需现场再次配置。
9. **云端收件箱自动刷新**：待确认电脑和待分配设备均每 10 秒刷新，同时保留管理员尚未提交的名称与车间草稿。
10. **文档入口去歧义**：明确新版交付是可复用客户 ZIP + 单文件 EXE + SYSTEM 任务计划；Python/SCM 服务章节仅为旧试点维护参考。
11. **长时间首次断网可恢复**：SYSTEM 后台任务持续重试登记；同机申请身份稳定，不依赖一次性包过期后重新下载。
12. **本地控制台兼容云端前端**：本地模式对云端待确认电脑和候选设备收件箱返回明确空列表。

## 已验证证据

- Python 完整回归：共发现 201 项，其中 179 项通过，22 项 PostgreSQL 测试在普通运行中按配置跳过；这 22 项随后在隔离 PostgreSQL 中全部通过。
- 隔离 PostgreSQL 16：数据库脚本连续初始化两次成功；2 项登记集成测试覆盖“自动报到→待加入列表→审批→同机领取独立凭据→错误申请密钥拒绝”以及“拒绝→同代禁止重试→轮换后旧包失效→新包允许同机重新申请”；原有 20 项真实事务、并发、租约与队列测试也全部通过。
- 迁移演练：人为降级为旧版同名非唯一索引并插入两个活动扫描任务后，重新迁移得到 1 个活动任务和唯一索引。
- 登记演练：同一安装 ID 重试保持同一待加入记录；审批前无上传凭据；审批后同机取得确定且独立的凭据；错误申请密钥得到 HTTP 401。被拒绝的电脑不能用原包反复申请；管理员轮换后，旧包得到 HTTP 401，同机使用重新下载的新包可重新申请。
- JavaScript 运行测试：国际化 7 项、邮件设置 10 项、连接状态 3 项、存储状态 3 项通过。
- Python 编译和 `node --check public/app.js` 通过。
- 云端 Docker 镜像 `hawkhive-cloud:0.5.0-preflight` 已从当前源码成功构建（本机产物为 `linux/arm64`）；这证明镜像构建链和文件复制通过，不代表已生成服务器所需架构、已经推送、部署或替换正式云端。
- 隔离本地云端真实浏览器路径已验证简化后的流程：自动报到电脑只显示主机名、Windows 版本和最近报到时间，不显示配对码；管理员将 `WIN-AUTO-QA` 改名为 `Home Workshop Collector` 并点击“加入并启用”后，待加入记录消失并生成独立采集节点。此前还已验证独立取证、HTTP 200 心跳、第二台电脑独立报到和拒绝；最终 Windows EXE 生成后仍需补做目标系统全生命周期验收。
- Playwright CLI 本身未运行：内部 npm 镜像返回 HTTP 500；本轮浏览器验收改用可用的本机浏览器自动化完成，不能把 CLI 失败写成通过。

## 仍阻断生产发布

1. **没有本轮源码对应的 Windows EXE**：目录中的旧 EXE 不包含 0.5.0 可复用登记流程，禁止作为最新版交付。需在 Windows VM/Windows CI runner 上执行 `build-windows-exe.ps1`，并将 EXE 与 `.sha256` 放入 `release/`。
2. **没有 Windows 生命周期实机证据**：需覆盖 UAC 安装、SYSTEM 无人登录启动、DPAPI 重启解密、重复双击、旧版升级、异常退出重启、断电恢复、卸载后数据保留。普通 Linux 容器不能替代这些验收。
3. **没有代码签名与信誉链**：未签名 EXE 可能被 SmartScreen 拦截；正式客户发布前需 Authenticode 证书、时间戳服务和签名后哈希留档。
4. **旧节点停用仍需受控入口**：新电脑可直接用通用包重新报到，不再需要重绑旧机器；但旧电脑损坏后，仍需增加云端“停用旧节点及凭据”的显式管理入口。
5. **没有升级回滚器**：重复安装可覆盖升级且保留数据，但尚未保存上一版 EXE并在新版本健康检查失败时自动回滚。
6. **自动扫描授权未落地**：当前会扫描所有活动 RFC1918 私有 IPv4 网卡各自的最多一个 `/24`，可能包含 VPN、虚拟交换机或非设备业务网。量产前需客户 IT 给出允许网段、频率与时段；否则只能称“受限自动探测”，不能称无条件全网发现。
7. **设备身份仍依赖网络端点**：协议资料没有已确认的唯一序列号寄存器。兼容协议设备可能成为候选，IP 变化会被当作新设备；必须使用静态 IP/DHCP 保留并由管理员确认车间和名称。
8. **正式云端尚未更新**：生产 `.env` 需新增稳定的 `DCP_ACTIVATION_SECRET`，镜像需打入已验收 EXE，再执行数据库备份、迁移、部署和登录后的真实下载验证。
9. **办公室现场当前不可达**：当前电脑已带回家，不能把家庭网络或本机测试当作 `192.168.2.30` 设备、车间 Windows 或正式云端联调证据。

## 下一轮验收顺序

1. 在隔离 Windows 11 64 位 VM/CI 构建 0.5.0 未签名候选 EXE，校验哈希和无 Python 环境启动。
2. 在两台 Windows 测试机用同一 ZIP 执行安装、双机独立配对、审批取证、重复启动、升级文件占用和任务异常重启测试。
3. 完成 Authenticode 签名，再重复病毒扫描、哈希记录和 Windows 安装测试；签名前后的哈希必然不同。
4. 将签名后 EXE/sidecar 打入云端镜像，先备份 PostgreSQL，再迁移和部署。
5. 用管理员浏览器真实执行“下载一次通用 ZIP -> 两台 Windows 安装并自动报到 -> 分别决定加入或拒绝并按需改名 -> 候选设备出现 -> 选择车间和改名”。
6. 回到车间后，对真实 DPC8001-G 做连续采集、断网积压、恢复补传、重复数据和断电恢复验收。

## 只需厂家或客户确认的问题

### 厂家

1. 是否有可读且跨重启、跨 IP 永久稳定的序列号/唯一 ID 寄存器；地址、长度、类型和字节序是什么？
2. DPC8001-G 同时允许多少个 Modbus TCP 客户端？厂家上位机和 HawkHive 同时连接是否会互相断开或返回旧帧？
3. 实际交付设备是否可能使用非 502 端口或非 1 Slave ID？若可能，允许范围是什么？

### 客户及客户 IT

1. 首批现场 Windows 的精确版本、64 位架构、域/GPO/杀毒软件限制，以及是否强制传统 Windows Service 或 MSI。
2. 每台采集电脑获准扫描的物理网卡、CIDR、扫描时段和频率；是否存在 VPN、Hyper-V、Docker、VMware 等虚拟网卡。
3. 设备 IP 是否全部设置静态地址或 DHCP 保留，且车间交换机/VLAN 是否允许 Windows 到设备 TCP 502。
4. 旧采集电脑损坏时，哪些角色可批准重绑？是否要求双人复核和多久的审计记录。
5. 是否接受“发现后只选车间并修改设备名称”这一步人工确认；在没有唯一序列号寄存器前不能安全取消该步骤。

## 可复用原则

自动发现只负责产生带协议证据的候选，不等于设备身份确认或自动入库。这个原则适用于缺少可验证唯一身份的 Modbus/PLC/仪表；若设备能提供经过厂家保证的唯一序列号并且云端已有序列号到车间的资产映射，才可以安全地进一步自动分配。反例是仅凭 TCP 502 端口开放就自动入库，这会把网关或其他兼容设备误当成目标设备。
