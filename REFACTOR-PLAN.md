# DCP8001 采集平台重构路线

## 目标架构

当前阶段保持两个可部署单元，不拆成微服务：

1. Windows 边缘采集器：设备轮询、数据校验、本地 SQLite、可靠上云队列、本机管理页。
2. 云端模块化单体：边缘接入、客户查询、配置管理、PostgreSQL、备份和健康检查。

单站点或少量客户阶段，微服务会增加部署、追踪、重试和数据一致性成本，却不会改善设备读取可靠性。只有当团队、发布节奏或负载已经需要独立扩缩容时，再拆独立服务。

## 不可破坏的约束

- 生产上云数据必须同时满足 `source=device`、站点匹配和租户匹配。
- `record_uuid` / `event_uuid` 是幂等键；上传采用至少一次投递，云端重复写安全。
- 采集时固化 `customer_id`、`site_id`、设备和房间标识，不能在上传时给旧数据重新贴标签。
- 模拟数据只用于本地沙箱，不得进入历史、最新值或报警上云通道。
- 云端离线不得影响本地采集；设备离线不得被显示成云端离线。
- “线程运行”“HTTP 可达”“数据已确认”是三个不同状态。

## 已落地的第一阶段

- 默认真实设备模式，模拟模式改为显式启用。
- 边缘和云端同时校验数据来源；报警也携带来源和站点。
- 本地管理写接口增加本机 Host、同源、JSON 和请求令牌校验。
- 云同步状态区分 connecting、connected、error、attention 和 stopped。
- 400/422 坏批次自动拆分；坏记录达到阈值后隔离，正常数据继续上传。
- 网络超时和 5xx 不消耗记录级隔离次数；重试加入随机抖动。
- 远程私网扫描默认关闭，仅允许现场显式启用。
- 源码发布包采用白名单生成，不包含 `.env`、令牌、数据库、WAL/SHM 或备份。

## 第二阶段：按职责拆模块

建议在不改变部署拓扑的前提下，逐步把大文件拆为以下边界：

```text
edge/
  domain/          # Reading、Alarm、QualityFlag、DeviceCapability
  adapters/        # DCP8001 Modbus TCP/RTU 适配器
  application/     # Poller、AlarmEvaluator、ConfigApplier、Uploader
  infrastructure/  # SQLite、迁移、HTTP 客户端、日志
  web/             # 本机只读状态与受保护管理接口
cloud/
  routers/         # edge_ingest、customer、admin
  services/        # 配置、查询、导出、设备开通
  repositories/    # PostgreSQL 查询和事务
shared/
  contracts/       # 版本化请求模型和状态枚举
```

拆分顺序应从纯模型和存储接口开始，再移动调用方；每一步保持现有入口可运行，避免一次性重写。

## 后续优先项

1. 引入显式数据库迁移版本，替换运行时零散 `ALTER TABLE`。
2. 为设备增加能力声明，例如 `temperature_supported`、`humidity_supported`，未配置的传感器不参与报警。
3. 对 10 / 30 / 50 台真实设备执行分级负载测试，基于连接时延和错误率调整已落地的有限并发与按设备退避参数。
4. 为云端设备、房间、站点增加同租户复合约束，并在接入前批量校验目标设备。
5. 将隔离记录做成只读诊断页，提供错误原因、首次/最后失败时间和人工重试操作。
6. 登录限流迁移到共享存储；为配置变更、发现任务和令牌操作增加审计日志。
7. 大数据导出改为流式或异步任务，避免一次性载入内存。

## 发布验收门

每次交付分别记录以下状态，不能互相替代：

1. 源码检查：变更范围和迁移兼容性已审查。
2. 测试：单元、协议、失败队列和安全边界测试通过。
3. 构建：源码包或容器可重复生成，且扫描不到运行数据和凭据。
4. 发布：产物有版本和 SHA-256。
5. 部署：指定 Windows 边缘端与云服务器已经更新并可回滚。
6. 真实路径：设备读数响应明确为 `source=device`，云端对应站点能看到同一时间戳和数值。

当前仓库已完成本地源码、122 项回归、源码包、Windows 11 旧版 EXE 无 Python 启动，以及 `192.168.2.30` 的真实只读采集和隔离 SQLite 落盘。发现探针修复后的 EXE 尚待 Windows 复验；云端仍未正式部署，设备屏幕到远程页面的完整端到端验收也尚未完成。
