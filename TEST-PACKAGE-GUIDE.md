# DPC8001-G Windows 测试包使用说明

版本：2026-09-04  
用途：在 Windows 电脑上进行界面、局域网发现、真实设备读取、本地保存和云端补传的试点验证。

## 重要边界

- 这是需要预装 Python 的测试包，不是签名的量产 EXE/MSI。
- 厂家已确认 DPC8001-G 的通信定义以独立《通讯协议》为准；测试包中的解析代码已经采用该定义。
- 模拟模式的数据不会上传云端，也不能用于现场或生产判断。
- 真实设备模式默认只读。不要使用诊断 CLI 的设备写入参数。
- 测试数据独立保存在 `%LOCALAPPDATA%\HawkHive\DPC8001-Demo-Test` 或 `%LOCALAPPDATA%\HawkHive\DPC8001-Device-Test`，不会覆盖正式服务的 `%ProgramData%` 数据。

## 一、准备环境

要求 Windows 10、Windows 11 或 Windows Server 2019 及以上，以及 64 位 Python 3.10 或以上。安装 Python 时勾选 Python Launcher。

依次双击：

1. `01-INSTALL-TEST-DEPS.cmd`：在包内建立独立 `.test-venv` 并安装测试依赖。
2. `02-RUN-SELF-CHECK.cmd`：执行编译，以及采集协议、诊断保护、存储、同步、Windows 启动器和前端契约测试。云端权限边界由源码全量回归单独覆盖。

只有自检全部通过后，再开始界面和真实设备测试。

## 二、无设备演示

双击 `03-START-DEMO.cmd`。第一次启动需要设置至少 12 位的测试密码，用户名固定为 `tester`。

浏览器会打开本机采集控制台：

```text
http://127.0.0.1:8787/collector.html
```

验收重点：

- 页面明确显示“模拟数据”，不会伪装成真实设备。
- 客户工作台能够登录，实时、历史、报警、场所设备、设置和大屏均能打开。
- 修改洁净室或设备显示名称后能够保存，并在相关页面同步显示。
- 关闭浏览器不会停止后台采集；关闭启动命令窗口才会结束本次测试。

## 三、真实 DPC8001-G 测试

1. Windows 电脑与设备接入同一个局域网，确认电脑可以访问设备 TCP 端口 `502`。
2. 双击 `04-START-DEVICE-TEST.cmd`。
3. 打开“扫描网络”，选择设备所在网卡对应的 `/24` 网段。
4. 候选设备必须同时通过实时寄存器、固件寄存器和粒子单位 `133` 校验，才能添加。
5. 人工核对设备屏幕、设备 IP、安装位置和页面读数后，再保存设备归属。

启动本地控制台后，不要再用另一个命令窗口对同一设备运行
`dcp8001_collector.py --loop`。部分设备只允许一个稳定采集会话，并发直读可能造成
超时或连接重置。请使用采集器页面内的“测试连接”，它会与后台轮询共用设备互斥锁。

当前只接受寄存器 `133=1`，即 `PCS/28.3L`。其他单位会明确失败，不会静默换算。

## 四、云端上传测试

需要项目方提供专用测试环境的 HTTPS 地址、站点 ID 和测试令牌。不要把生产令牌用于首次联调。

在本机控制台展开“配置云端连接”，填写测试参数并执行连接测试。随后验证：

1. 正常联网时，本地真实读数能够在远程页面出现。
2. 断开互联网 30 分钟后，本机仍持续采集，待上传数量增加。
3. 恢复互联网后积压逐步清零，云端没有重复记录。
4. 模拟模式数据始终不会进入上传队列。

## 五、必须记录的测试结果

- Windows 版本、Python 版本、测试包 SHA-256。
- DPC8001-G 固件版本、IP、端口、Slave ID、寄存器 `133` 原始值。
- 同一时刻的设备屏幕、采集器页面和云端页面截图。
- 断网开始/恢复时间、最大待上传数量、补传完成时间。
- 所有失败步骤的页面提示和命令窗口完整错误文本。

## 六、尚不能由本测试包证明

- Windows 服务无人登录启动、断电恢复、DPAPI 与 ACL，需要按 `WINDOWS-DEPLOYMENT.md` 另做管理员安装验收。
- 代码签名、EXE/MSI、自动升级和失败回滚尚未完成。
- 独立《通讯协议》内部仍需厂家澄清：寄存器 `125` 的工作模式枚举、寄存器 `126` 的默认值，以及地址 `0` 广播读取的碰撞规则。
- 没有真实设备、客户网络和正式云端证据时，不能把本机自检通过称为量产验收通过。
