# DPC8001-G Windows 测试包验证记录

验证日期：2026-09-04  
验证对象：`DPC8001-G-Windows-Test.zip`

## 本轮结论

测试包已达到“交给 Windows 现场做试点测试”的条件，但不是量产安装包，也不能替代真实 DPC8001-G、客户网络和正式云端验收。

本轮真实页面验收发现并修复一项缺陷：客户工作台点击“大屏”时，通用导航事件错误处理了外部链接，导致原工作台内容被隐藏。现只给具有 `data-view` 的内部导航按钮绑定视图切换，并增加回归测试。

## 已验证

| 项目 | 结果 | 证据 |
|---|---|---|
| 全量 Python 回归 | 通过 | 2026-09-04 当前源码 122 项测试全部通过 |
| Python 编译 | 通过 | 全部根目录 Python 文件可编译 |
| 前端脚本语法 | 通过 | `public/*.js` 全部通过 Node 语法检查 |
| 云端容器构建 | 通过 | `hawkhive-cloud:local` 构建成功 |
| PostgreSQL + 云 API 启动 | 通过 | 两个容器健康，`/api/v1/health` 返回 `{"ok":true}` |
| 桌面真实点击 | 通过 | 登录、实时、历史、报警、场所设备、设置保存、本机采集器、大屏 |
| 移动端 390×844 | 通过 | 本机采集器和客户实时页无空白、遮挡或控制台错误 |
| 中英文切换 | 通过 | 客户工作台、本机采集器和大屏均能切换 |
| 独立解包验证 | 通过 | 既有包内 `MANIFEST.sha256` 全部通过；本轮会重新生成清单，解包后仍须运行包内自检 |
| Windows 单文件 EXE | 通过 | Windows 11 x86-64 构建；卸载 Python 后 EXE 仍可启动并打开本机页面 |
| 局域网候选发现 | 通过 | 2026-09-04 修复旧事务帧和半字段读取后，`192.168.2.30:502` 通过 MBAP、协议寄存器和单位验证 |
| 真实采集与本机落盘 | 通过 | 后台监控服务从 `192.168.2.30` 读取六通道、温湿度和单位，写入隔离临时 SQLite；记录保持 `synced_at=NULL`，可供网络恢复后补传；临时数据已清理 |
| 默认节奏短时稳定性 | 通过 | 同一长连接按 10 秒间隔只读 12 次，12/12 成功、无错误，单轮延迟 607–1739 ms（中位数 743 ms） |
| 敏感运行数据排除 | 通过 | 不包含 `.env`、令牌配置、SQLite、WAL、备份、转储或本地虚拟环境 |

## 静态检查说明

Ruff 仍报告 87 条既有告警，主要是 FastAPI `Depends` 默认参数、宽泛异常兜底、类型异常类别和导入风格。它们没有造成编译或测试失败；本轮没有为了清零静态数字批量重写稳定业务路径。后续应单独建立静态规则基线并逐类处理，不能把这些告警解释为“零风险”。

## 尚未验证

- 已在 Windows 11 `10.0.22631.7219` 验证单文件 EXE、自动开页和无 Python 启动；尚未覆盖 Windows 10/Server、包内全部 PowerShell 脚本和域策略环境。
- 未在 Windows 服务模式验证无人登录启动、断电恢复、LocalService、DPAPI、ACL、卸载保留数据。
- 已在真实局域网读取 `192.168.2.30:502` 的标准 Modbus TCP 数据，并验证本机 SQLite 落盘及待上传状态；设备当前停采，尚未核对启动后的动态粒子值、设备屏幕与云端页面。
- 已验证当前活动 WLAN 自动扫描；尚未覆盖双物理网卡/VLAN、防火墙策略、断网 30 分钟和补传。
- 未连接正式云服务器；当前云端证据来自隔离 Docker 环境。
- 尚未制作签名 EXE/MSI、自动升级与失败回滚。

这些未验证项必须按 `TEST-PACKAGE-GUIDE.md` 和 `WINDOWS-DEPLOYMENT.md` 在现场留证后，才能进一步判断是否达到量产条件。
