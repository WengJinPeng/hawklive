# Wi-Fi 断连恢复检查（2026-09-09）

## 现场事实与边界

本轮通过已登录的 `https://avi.hawk-hive.com/` 云端后台刷新核对，检查期间：

- “第二台采集电脑” `DESKTOP-PAHQTGT` 仍有新鲜心跳，自报版本 `0.3.0`，配置 `v6` 已应用。
- `Cleanroom 2 / Device 1 (CR2)` 登记地址为 `10.12.0.140:502`，Slave ID 为 `1`。
- 最后有效读数为北京时间 `2026-09-09 17:16:51`；后续检查时仍未推进，页面显示已经过去约 13 分钟。
- 采集器错误摘要为 `Socket closed while waiting for initial Modbus TCP data`。
- 待上传为 `0`；本地剩余磁盘约 `9.6 GB`，显示空间告警。队列为空不能证明设备断网期间的数据已被采集。
- 该节点最近 24 小时诊断日志查询为空。旧版未提供本轮所需的逐次重连日志，不能据此还原每次失败时间或设备内部状态。
- Primary Site 自报 `0.5.2`，未上报设备；本轮故障不能用它的版本或心跳代替第二台电脑的证据。

以上支持“采集电脑仍可联系云端，但设备通信失败”。错误发生在 TCP 连接建立后的初始化等待阶段；此时尚未发送当前 Modbus 请求。它不证明 Wi-Fi 断开、IP 变化或固件卡死中的某一个就是唯一根因，也不代表下述本地缺陷全部曾在现场触发。

直接 SSH 的现有默认密钥未通过认证；本轮未读取服务器底层日志或执行生产变更，现场事实来自已认证云端页面。

## 本地源码修复

- 重连退避默认由最长 300 秒改为 `2 / 4 / 8 / 10 / 10…` 秒，每次失败关闭失效连接，再建立新连接。等待时间不含实际连接/读请求耗时；已有环境变量覆盖仍需在现场检查。
- 为一整条样本的多次寄存器读取设置总时限，保留小报文、单位检查与事务号校验。默认连接超时 5 秒；连接建立后整条样本最多约 11 秒，单个请求仍最多 5 秒。
- 多设备调度独立回收已完成任务，同一设备最多一个正在执行的后台轮询；慢设备不会阻止空闲 worker 为其他设备采集。
- 已删除、停用或更换端点的登记不再长期占用空闲缓存连接，避免挤占设备网口的会话容量；在途读取完成后再清理。
- 短暂配置读取异常会记录并重试，不再直接结束监控线程。
- 检测到断线时保存最后一条真实收到但尚未保存的样本，保留原采集时间；恢复首条立即保存，不再等 120 秒常规历史周期。重复失败不重复保存，不生成缺失时段的估算数值。

## 验证方法

`test_device_recovery.py` 使用隔离 SQLite、真实本地 TCP socket 和可控 Modbus 应答器，覆盖：

1. TCP 不关闭但不回数据，超时后释放旧连接，同一监控线程自动接续新连接。
2. TCP 接受后在初始化等待阶段关闭，复现现场错误文字，设备重新接受连接后自动恢复。
3. 每个寄存器都在单次超时前回复，但完整读取过慢，整条样本总时限仍生效。
4. 慢设备不阻塞健康设备继续轮询，同一设备不并发开多个后台会话。
5. 配置读取暂时失败后继续采集。
6. 连续失败重试间隔有上限，失败期间不造数。
7. 断线前最后样本、恢复首条样本按原时间写入待上传队列，UUID 不重复。
8. 删除/停用登记后释放旧网口会话。

本轮完整 Python 回归发现 274 项，214 项通过，60 项因 PostgreSQL/Windows/显式启用的集成环境条件未具备而跳过；其中上述 8 项恢复测试均通过。受影响 Python 文件编译与 `git diff --check` 通过。首次尝试的旧临时虚拟环境缺测试依赖，已换用依赖齐备的隔离测试环境重跑；未修改生产依赖或生产服务。

这些是本地故障模拟，不能替代现场 Windows、真实 Wi-Fi 模块或设备固件验收。

## 现场完成条件

本轮源码改动尚未制作并安装对应的 Windows EXE；云端页面上的 `0.3.0` 采集器不会因本地源码修改而变化。升级需沿用该电脑现有节点身份、配置和 SQLite 数据目录，并保留回退包。

安装修复版后，在保持设备与采集器运行的情况下中断设备 Wi-Fi 5–10 秒，再恢复；核对同一采集器进程自动恢复真实读数、云端时间推进、断线前后样本可查和待上传队列清零。另测试约 5 分钟的设备断网，以及仅云端出口断网后本地积压补传；二者不能混为同一种测试。

如恢复 Wi-Fi 后端点仍立即关闭 TCP，应检查是否有另一采集程序占用，以及网口模块残留会话的释放规则。如 IP 变化，需核对设备身份后更新登记；不能扫描到另一台设备就自动替换。

仍需厂家确认的一项关键能力：DPC8001-G 是否在自身断网期间缓存带采样时间/序号的历史数据，并提供可只读补采的协议、容量、覆盖及断电保留规则。现有采集路径只读实时寄存器；没有该能力就无法恢复从未收到的测量值。
