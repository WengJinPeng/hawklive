# DPC8001-G Windows 车间边缘采集器产品需求文档（PRD）

版本：V0.2（源码复核稿）  
日期：2026-09-03  
适用范围：已确认型号为 `DPC8001-G` 的只读数据采集场景；其他型号必须另建适配和验收范围

## 1. 产品结论

本产品不应被定义为一个“扫描后上传的小工具”，而应被定义为部署在车间网络边界的 **Windows 边缘采集服务**：它持续发现和识别受支持的设备，可靠采集数据，先在本机持久化，再通过出站 HTTPS 幂等上传到云端，并向现场人员与远程人员分别提供适合其职责的状态和操作入口。

首版最重要的不是功能数量，而是以下四件事：

1. Windows 重启、无人登录、浏览器关闭后仍持续采集。
2. 断网、云端故障、设备离线时不静默丢数据，也不混淆故障来源。
3. 自动发现只产生“候选设备”，经过协议和数据校验后才允许纳管，不能把任意开放 502 端口的主机当作目标设备。
4. 云端只能通过采集器主动发起的出站连接获取数据和下发受控配置，默认不开放从公网进入车间局域网的通道。

## 2. 已知事实、假设与待确认项

### 已验证或已有代码支持

- 当前项目已支持 DPC8001-G 协议资料对应的 Modbus TCP/RTU 读取、本机 SQLite、后台轮询、局域网扫描、设备连接测试、可靠上云队列、云端配置拉取和基础监控页面。
- 当前扫描范围限制在私有 IPv4 的单个 `/24` 网段，最多 254 个地址，默认探测 TCP 502 和 Slave ID 1。
- 当前历史数据使用稳定 `record_uuid`，云端按幂等键去重；临时网络错误重试，永久坏数据可隔离。
- 当前项目只完成过本地代码、测试、构建和部分真实设备链路验证；尚不能据此认定 Windows 生产部署、云端生产部署和完整现场端到端验收已经完成。

### 本 PRD 的暂定假设

- V1 只支持 `DPC8001-G`，默认 Modbus TCP 只读采集。`S`、`X-G`、`X-S` 和其他系列不因说明书合并列出而自动获得支持。
- 每个车间或站点至少部署一个 Windows 采集节点；一台节点负责同一设备网络中的多台设备。
- 现场 Windows 电脑可主动访问云端 HTTPS，但云端不能主动连接设备网。
- 技术人员负责首次安装和异常维修；非技术人员负责日常查看，不负责 IP、端口、Slave ID、令牌和数据库维护。
- 采集器不向设备写寄存器，不承担设备启停、参数下发或固件升级。

### 开发前必须确认

1. 已确认目标型号为 `DPC8001-G`；仍需确认现场全部设备是否均为该型号，以及是否混有其他品牌、Modbus RTU、OPC UA、串口或厂家私有协议设备。
2. 单车间典型/最大设备数分别是多少？需要 10 秒实时值，还是每一条 10 秒采样都必须永久保存？
3. Windows 版本、是否允许管理员安装服务、是否可能休眠/断电、是否有杀毒软件或域策略限制？
4. 设备网是否可能跨 VLAN、多网段，或 Windows 同时使用 Wi-Fi 上网和网线连接设备？
5. 最长可能断网多久，本机至少要保存多少天，监管或客户要求保留多少年？
6. 设备能否读取序列号/型号/固件版本等稳定身份？如果不能，IP 变化时如何确认“还是原来那台设备”？

### 资料基线与冲突处理

- 独立《通讯协议》用于实现报文、寄存器和字序；设备说明书用于设备能力、安装与维护；《上位机使用指南》用于核对最低业务流程。
- 已知 `12–18`、`24–27`、`129` 和粒子单位表述存在冲突。冲突字段在厂家按具体型号和固件确认前不得靠猜测扩展，更不得开放远程写入。
- 厂家上位机指南中的 RS485 地址扫描不是 LAN IP 自动发现；两条用户路径必须分开设计和验收。

## 3. 用户与职责

| 角色 | 主要任务 | 可以操作 | 不应操作 |
|---|---|---|---|
| 现场安装/维修人员 | 安装、绑定、发现设备、诊断网络 | 网卡/网段、设备连接、采集器绑定、日志导出 | 客户业务数据、跨站点配置 |
| 车间值班人员 | 判断是否正常、处理现场异常 | 查看状态、确认现场已检查、联系维修 | 修改底层通信参数 |
| 办公室/质量人员 | 远程查看、追溯、导出、处理报警 | 实时、历史、报表、报警闭环 | 进入现场局域网、控制设备 |
| 企业管理员 | 人员、站点、阈值、通知、审计 | 业务配置和权限 | 直接读取边缘机文件或令牌 |
| 平台运维人员 | 开通租户、站点、边缘节点、版本运维 | 吊销令牌、诊断同步、发布升级 | 默认查看客户明文业务数据 |

现场维护工具与远程业务工作台必须分开。非技术用户不需要理解 SQLite、同步队列、IP、端口、Slave ID 或 Modbus。

## 4. 目标与成功指标

### 用户目标

- 非技术人员打开远程工作台后 10 秒内知道“正常、需要处理、还是数据暂不可用”。
- 技术人员用安装向导在 10 分钟内完成一个标准单网段站点的绑定、发现、验证和启用。
- 网络或云端恢复后，无需人工导文件即可自动补传积压数据。

### 建议 SLO（需结合现场规模校准）

- 采集完整率：设备和网络正常时，24 小时内成功采集周期占比不低于 99%。
- 实时新鲜度：正常网络下，云端展示时间比现场采样时间滞后不超过 30 秒。
- 本地可靠性：采集成功的数据必须先提交本地事务，再进入上传；进程崩溃后不得出现“已显示成功但未落盘”。
- 云端去重：相同 `record_uuid` 重传任意次数，云端只产生一条永久记录。
- 自动恢复：Windows 服务异常退出后 60 秒内重启；Windows 重启且无人登录时 2 分钟内恢复采集。
- 失联识别：设备离线、采集器离线、云端不可达三类状态分别显示，不得统一显示为“无数据”。

## 5. 产品范围与系统边界

```text
受支持设备
  │ Modbus TCP/RTU（局域网，只读）
  ▼
Windows 边缘采集服务
  ├─ 设备发现与协议适配
  ├─ 采集、校验、质量标记
  ├─ SQLite 本地库 + Outbox 上传队列
  ├─ 本机维护页（仅 localhost）
  └─ Windows Service、日志、健康与升级
  │ 出站 HTTPS（数据上传、心跳、配置拉取）
  ▼
云端接入 API → PostgreSQL → 远程工作台/报警/报表
```

核心层级必须是：`客户 → 站点/车间 → 采集器 → 区域/房间 → 设备 → 数据`。IP 地址只是可变连接端点，不能充当设备永久身份。

## 6. P0：首版不可缺少的需求

### P0-1 Windows 安装与无人值守运行

需求：

- 提供带版本号的安装包，完成运行时、程序、数据目录、Windows 服务和卸载入口安装。
- 采集进程注册为 Windows Service，开机自动启动，不依赖用户登录、浏览器或命令窗口。
- 使用专用低权限服务账号；同一台电脑只允许一个采集实例写同一数据库。
- 服务异常退出后自动恢复，并对连续崩溃进入“需要人工处理”状态，避免无限重启掩盖故障。
- 数据、配置和日志写入 `%ProgramData%` 下的独立目录，不写入程序安装目录。
- 安装向导检查管理员权限、Windows 版本、磁盘余量、休眠策略、防火墙和到云端的 HTTPS 连通性。
- 部署清单明确要求电脑在生产时段持续供电、禁止自动睡眠；根据现场断电风险决定是否配 UPS。
- 如果使用双网卡，设备网与办公/互联网网络不得开启系统网桥或 IP 转发；防火墙仅放行到已授权设备网的采集端口和到云端的出站 HTTPS。
- Windows 更新、杀毒扫描和自动重启必须有维护窗口，不能在生产时段无提示中断采集。
- 最低 CPU、内存和磁盘规格以目标设备数的负载测试结果确定，不能只给一个未经验证的固定配置。

验收：

- Windows 重启且无人登录，采集和上传自动恢复。
- 退出浏览器、注销当前用户不影响采集。
- 手动终止进程后服务按策略恢复，并在云端留下中断和恢复时间。
- 重复启动第二实例时被明确阻止，不产生双重采集。
- 电脑进入睡眠、断电或计划重启后，云端能识别采集器失联；恢复供电后服务自动恢复并补传。

### P0-2 首次绑定与非技术化安装向导

需求：

- 云端提供同一客户可重复使用的 Windows ZIP，不要求管理员预先逐台创建节点或生成一次性包。
- 每台电脑生成稳定安装身份和独立申请密钥，启动后自动进入云端待加入列表；管理员决定加入并按需修改采集器名称后，云端才签发该电脑独立的长期上传凭据，无需人工抄录配对码。
- 向导分为：环境检查 → 自动报到 → 云端确认电脑 → 选择设备网卡/网段 → 扫描 → 验证设备 → 命名/归属 → 启用 → 完成检查。
- 通用安装包只具备创建待确认申请的权限，不能读取或上传业务数据；误发时可轮换，使旧包和全部未确认申请失效，但不得中断已批准采集器。
- 云端地址可配置，但生产环境只允许 HTTPS；变更地址时必须测试证书、身份和站点匹配后再生效。
- 错误提示必须包含“发生了什么、数据是否还安全、用户下一步做什么”。

验收：

- 标准环境下，新安装人员不编辑配置文件即可完成部署。
- 错误客户登记、错误机器申请密钥、被轮换的安装包或错误云端地址不会覆盖原有有效配置。
- 同一 ZIP 在两台新电脑安装时，两台电脑必须产生不同配对记录并在批准后取得不同上传凭据。
- 配置修改采用原子写入；断电或崩溃不会留下半份配置。

### P0-3 多网卡与受控设备发现

需求：

- 列出 Windows 当前网卡、IPv4、网关、网段和状态，让安装者明确选择“设备网络”。
- 支持一个或多个明确授权的私有网段；扫描范围和端口必须受限，默认不得扫描公网。
- 首次安装主动扫描；后续周期扫描默认只用于发现变化，不自动纳管新设备。
- 发现分为四级：IP 可达、端口开放、Modbus 握手成功、目标型号/数据格式验证成功。
- 只有通过目标协议指纹和真实寄存器数据校验的设备才显示为“可添加”；仅端口开放的主机显示为“未知候选”。
- 设备加入前要求确认名称、区域/房间、设备类型；支持批量选择但不得自动猜测业务归属。
- 对重复端点、重复序列号、IP 冲突、已纳管设备 IP 变化给出不同处理流程。

验收：

- Windows 使用 Wi-Fi 访问云端、网线连接设备时，能够扫描正确的有线网段。
- 网段内存在非目标 Modbus 设备或普通 502 端口服务时，不会被自动启用采集。
- 已纳管设备换 IP 后，如果有稳定设备身份则更新端点并保留历史；没有稳定身份时必须要求人工确认，不能生成静默重复设备。
- `/23`、VLAN 或多个网段的场景必须明确支持方式或给出可理解的限制提示，不能悄悄只扫 `/24`。

### P0-4 协议适配与只读采集

需求：

- 每种型号使用独立、版本化的协议适配器，声明传输方式、寄存器、字节序、量程、单位和支持能力。
- 首版默认只允许读取；写寄存器、启停设备和固件升级不进入客户版本。
- 每条数据至少记录：客户、站点、采集器、区域、设备、设备端点、采集时间、接收时间、协议版本、原始值/解析值、单位、来源和质量标记。
- 数据质量至少区分：有效、超量程、协议异常、值冻结、时间异常、缺少能力、模拟数据。
- 同一设备的采集串行化，不同设备有限并发；离线设备指数退避，不能拖慢全部设备。
- 支持采集超时、重试次数、永久历史间隔的受控配置，并设置安全上下限。

验收：

- 用厂家协议样本和真实设备对全部字段、字节序、倍率、单位逐项比对。
- 单台设备超时不会阻塞其他设备；离线恢复后自动重新加入正常轮询。
- 未支持的传感器不显示为 0，也不参与报警，而显示“该设备不支持”。
- 模拟数据具有强标识，不能写入生产历史、最新值或报警上云通道。
- 自动发现只有在目标协议寄存器和粒子单位都验证通过后才允许添加；“端口可达”“Modbus 有响应”不得显示成已验证设备。

### P0-5 本地持久化与断网保护

需求：

- 采用“先本地事务提交，后异步上传”的 Store-and-Forward 模式。
- 永久历史、最新值、报警事件和上传状态分开管理；上传确认前不得删除待上传记录。
- 本地库启用 WAL、完整性检查和显式 schema migration；升级失败可回滚到上一版本。
- 明确定义本地保留策略：待上传数据不可因普通清理而删除；已同步数据按天数/容量归档。
- 建立磁盘水位：预警、严重、保护三档。任何丢弃策略必须显式报警并留下审计，禁止静默覆盖未上传数据。
- 定期生成本地备份并实际执行恢复演练；数据库损坏时保留原文件，禁止自动用空库覆盖。
- 所有时间以 UTC 存储，同时记录站点时区；监控系统时钟跳变和 NTP 偏差。

容量必须按公式核算：

`记录数 = 设备数 × 每天秒数 ÷ 永久保存间隔 × 最大离线天数`

例如 100 台设备、每 120 秒保存一条、离线 30 天，约产生 216 万条永久记录；如果改成每 10 秒永久保存，则约 2592 万条，磁盘、查询和补传设计完全不同。

验收：

- 断开云端 24 小时后，本地持续采集；恢复网络后自动补传，云端无重复且可核对记录数量。
- 在写库、上传、升级时分别模拟进程崩溃和断电，重启后队列状态一致。
- 模拟磁盘不足和数据库损坏，系统明确告警，不把空库或旧值伪装为正常实时数据。
- 已同步清理不会删除未同步或隔离待处理记录。

### P0-6 可靠上传与配置同步

需求：

- 采集器仅主动访问云端 HTTPS；上传接口使用站点绑定的边缘身份，服务端再次校验客户、站点、设备和来源。
- 历史数据采用至少一次投递，云端按 `record_uuid` 幂等；报警事件使用独立 `event_uuid`。
- 批量、压缩上传；超时、断网、429、5xx 使用带抖动的指数退避，避免大量站点同时重连冲击云端。
- 400/422 等永久数据错误隔离到死信队列，不阻塞正常记录；隔离记录可查看原因并受控重试。
- 上报心跳至少包含版本、设备在线数、最后成功采集、最后云端联系、待上传数、最老积压、磁盘余量、时钟偏差和错误摘要。
- 远程配置采用“期望版本 → 本地校验 → 原子应用 → 回报结果”；失败时保留最后可用配置并说明原因。
- 删除/停用设备采用软停用，历史数据不随配置同步删除。

验收：

- 重复发送同一批数据不会产生重复行。
- 云端返回 503、429、超时、非法 JSON、部分 422 时分别得到正确的重试或隔离行为。
- 下发非法 IP、超出数量上限、错误站点或旧版本配置时，本地拒绝应用且继续使用旧配置。
- 云端不可达不影响本地采集，设备离线也不应被报告成“云端断线”。

### P0-7 安全边界

需求：

- 本机维护页面默认只绑定 `127.0.0.1`；如确需远程维护，应另行设计 VPN/零信任接入，不直接暴露端口。
- 边缘长期凭据使用 Windows DPAPI/Credential Manager 或等效机制保护，不能以明文 JSON 保存。
- 支持令牌轮换、吊销、站点解绑和设备丢失处置；原始令牌只显示一次。
- 云端 API 校验租户、站点、采集器和设备归属，客户端提供的客户 ID 不能成为授权依据。
- 配置变更、绑定、扫描、设备启停、令牌和升级均记录操作者、时间、旧值、新值和结果。
- 安装包和升级包必须签名并校验哈希；升级失败自动回滚，不能远程执行任意脚本。
- 日志脱敏，不记录令牌、密码、完整授权头或不必要的业务敏感数据。
- 设备通信采用只读最小权限；如果 Modbus 本身无认证，应通过网络隔离和允许列表限制风险。

验收：

- 非本机来源无法访问现场维护写接口。
- 从配置文件、日志和发布包中无法直接取得明文长期令牌。
- 被吊销采集器不能继续上传或拉取配置，但本地历史仍保留供授权恢复。
- 篡改的安装/升级包不会执行，失败后采集器回到最后可用版本。

### P0-8 状态、告警与可诊断性

需求：

- 分开显示：设备状态、采集服务状态、本地存储状态、云端连接状态、上传队列状态。
- 状态不得只写“在线/离线”，应包含最后成功时间、持续时长、影响范围和下一步动作。
- 云端对采集器心跳超时、所有设备离线、单设备离线、积压超限、磁盘不足、时钟异常和版本过旧分别告警。
- 现场提供“一键生成诊断包”，默认只含脱敏配置、版本、近期日志和健康信息，不包含令牌与完整业务数据库。
- 日志轮转并限制总容量；关键审计日志不能被普通业务用户删除。

验收：

- 拔掉设备网线、断开互联网、停止 Windows 服务、让磁盘不足，云端和本地分别给出正确故障类型。
- 故障恢复后保留发生、发现、恢复时间，不能只覆盖成当前“正常”。
- 非技术人员能够根据状态文案判断“是否影响数据、是否需要现场人员处理”。

### P0-9 远程工作台与数据可信度

需求：

- 办公室和值班人员通过 HTTPS 登录云端后，可按站点、区域和设备查看实时状态、历史趋势、报警和数据完整率。
- 首屏先显示业务结论：正常、需要处理、数据暂不可用；再展示最后采样时间、来源和详细数值。
- 最新值超过新鲜度阈值后必须显示“数据已过期”，不能继续用绿色状态把旧值伪装成实时值。
- 空状态必须区分：尚未采集、设备离线、采集器离线、云端处理延迟、筛选无结果和无权限。
- 用户、站点、设备、历史、导出和报警全部执行服务端租户隔离；页面隐藏不是权限控制。
- 所有时间以站点时区显示并明确标注；导出记录包含 UTC 时间、站点时间、设备身份、单位、来源和质量标记。
- 移动端优先支持查看结论、确认异常和联系现场人员；技术配置仍放在现场维护入口。

验收：

- 两个客户账号互相无法通过页面或直接 API 查询对方的站点、设备、历史和导出。
- 停止采集器后，页面在约定心跳窗口内从“正常”变为“采集器离线”，旧读数同时标记过期。
- 同一条真实记录能按设备 ID、`record_uuid`、采样时间和数值从本地库核对到云数据库及页面。
- 390px 手机宽度完成主要值班路径时无横向溢出，状态不只依赖颜色表达。

### P0-10 厂家上位机基线的范围决议

以下能力在厂家上位机指南中明确存在，但当前产品尚未全部实现，必须在试点范围确认会上逐项签署“本期实现/后续实现/明确不需要”：

- 后台服务是否需要 RS485 串口直连、串口扫描地址 `1–247`、串口/波特率持久化；当前生产服务仅完整接入 Modbus TCP。
- 报警策略是否需要“房间默认 + 单设备覆盖”；当前实现为房间级策略。
- 客户自助多用户管理及角色范围；当前只有认证和角色基础，无完整用户管理页面。
- 保存周期是否需要在界面配置为 1–100 秒/分钟，以及它与设备连续/间歇采样周期如何对齐。
- 自定义 Logo/文字、仪表盘布局、试用授权、历史手工删除是否属于本项目；不能因厂家旧软件具备就默认为新系统的 P0，也不能在未决时宣称范围完整。

验收：

- 形成带客户签字/邮件确认的范围矩阵；未决项不能进入“需求已完成”统计。
- 若要求 RS485，必须在目标 Windows 上以真实 USB/串口转换器、多从站、重复地址和断线恢复单独验收。
- 若要求设备级报警覆盖，必须验证默认继承、覆盖、恢复默认、权限、审计和历史报警不被配置变更改写。

## 7. P1：首版稳定后尽快完成

- 签名的自动升级、灰度发布、暂停、回滚和版本合规看板。
- 多协议插件框架与设备能力模型，逐个引入其他型号，而非做“万能自动识别”。
- 主采集器/备用采集器高可用，但同一设备同一时刻只能有一个租约持有者，避免重复采集。
- 本地数据加密、Windows 事件日志集成、企业代理和自定义 CA 支持。
- 远程下发“扫描任务”，但必须由现场预授权允许的网段和端口，且有频率限制和审计。
- 报警确认、指派、备注、复核、关闭和通知升级闭环。
- “房间默认 + 单设备覆盖 + 一键恢复默认”的报警策略，并保留配置版本和审计。
- 校准、保养、证书到期、设备固件和资产台账。
- 日报/周报/月报、班次/批次标签和完整率报表。

## 8. P2：规模化后考虑

- 多站点集中运维、设备模板、批量开通和策略继承。
- MQTT/OPC UA 等协议接入和边缘插件沙箱。
- 数据分层存储、长期归档、灾难恢复和区域化部署。
- 远程协助通道，但必须采用短期授权、用户可见、全程审计的零信任方案。
- 采集器高可用集群和跨节点自动接管。

## 9. 明确不做（V1 Non-goals）

- 不承诺发现或解析任意厂家、任意协议的设备。自动发现必须建立在已实现的设备适配器上。
- 不从公网直接扫描车间局域网，不在路由器上为采集器开放入站端口。
- 不做设备远程控制、写寄存器、固件升级和生产工艺参数修改。
- 不以 IP 地址作为设备唯一身份，不因 IP 变化自动创建新设备。
- 不承诺“绝不丢数据”。断电、磁盘物理损坏和超过容量上限都可能造成损失；产品必须定义容量、备份、告警和恢复边界。
- 不在 V1 拆微服务。当前规模下，Windows 边缘服务加云端模块化单体更容易保证一致性和运维质量。

## 10. 关键状态模型

### 设备状态

- `未配置`：已发现但未确认归属。
- `验证中`：正在做协议和数据校验。
- `正常`：最近采集成功且数据新鲜。
- `退避重试`：连续失败，降低轮询频率。
- `离线`：超过阈值未成功采集。
- `数据异常`：可通信但值无效、冻结、越界或解析失败。
- `已停用`：不再采集，历史保留。

### 云同步状态

- `未配置`、`连接中`、`正常`、`有积压`、`有隔离数据`、`认证失败`、`云端不可达`、`已停止`。

“服务线程正在运行”“HTTP 请求能够到达服务器”“一批数据得到业务确认”是三个不同事实，页面与监控不得混为一个绿色状态。

## 11. 交付阶段建议

### 阶段 A：协议与现场基线

- 确认设备清单、协议、稳定身份、网络拓扑、数据字段、单位、量程和采样策略。
- 用 1、10、30、50 台真实或硬件在环设备测量时延、失败率、CPU、内存、磁盘增长和补传速度。

### 阶段 B：可安装的 Windows 边缘产品

- Windows Service、安装向导、多网卡选择、受控发现、设备验证、本地存储、诊断和安全凭据。
- 完成重启、断网、断电、磁盘不足和数据库损坏测试。

### 阶段 C：云端闭环

- 站点开通、边缘绑定、可靠接入、心跳、远程状态、配置版本、报警和审计。
- 完成真实设备 → Windows → 公网 → 云数据库 → 办公室浏览器的同时间戳/同数值验收。

### 阶段 D：小范围试点后规模化

- 先选一个车间连续运行 2 至 4 周，记录缺数、误报、人工干预和资源占用，再推广其他车间。
- 试点期间保留人工对照记录，不以页面“看起来正常”替代设备原始值核对。

## 12. 上线验收矩阵

| 场景 | 必须证明的结果 |
|---|---|
| 正常运行 72 小时 | 采集完整率、云端新鲜度、CPU/内存/磁盘符合基线 |
| Windows 重启/注销 | 无人登录自动恢复，无重复实例 |
| 云端断网 24 小时 | 本地持续记录，恢复后完整补传且无重复 |
| 单设备/全部设备断线 | 只影响对应设备，故障原因与云端断线分开 |
| Wi-Fi 上网 + 网线设备网 | 选择并扫描正确网卡，不扫描错误网段 |
| 设备换 IP/地址冲突 | 保留稳定身份或要求人工确认，不静默重复 |
| 云端 429/5xx/422 | 分别退避、重试或隔离，不阻塞好数据 |
| 磁盘不足/数据库损坏 | 提前告警、保留证据、不伪装正常、不静默丢弃 |
| 非目标 502 服务 | 不会被识别成可采集目标设备 |
| 非法远程配置 | 本地拒绝并继续使用最后可用配置 |
| 令牌吊销/升级包篡改 | 拒绝访问或执行，且能够恢复到安全状态 |
| 真实端到端 | 同一设备的现场值、本地记录、云端记录和页面展示可按 ID/时间戳核对 |

## 13. 当前代码与目标需求差距

| 能力 | 当前判断 | 主要差距 |
|---|---|---|
| Modbus TCP/RTU 读取 | 已有基础实现；每个采集周期都会读取单位寄存器 `133`，当前仅接受 `PCS/28.3L`，并把单位与协议档案写入本地和云端 | 仍需多台真实设备、传感器能力声明、寄存器冲突勘误和固件版本验收 |
| 多设备后台轮询 | 已实现，有限并发和退避已有测试 | 尚无 10/30/50 台现场负载基线 |
| 局域网自动发现 | 部分实现；可枚举多网卡并由用户选择候选网段 | 单次仍限 `/24`、默认端口/Slave；只确认目标寄存器可读，厂家未提供强设备身份 |
| 本机数据保存 | 已实现 SQLite/WAL、每日在线备份、磁盘水位和“只清理已确认上传数据”的保留策略 | 缺少目标 Windows 上的断电/损坏恢复演练、备份外置和保护水位下的现场处置验证 |
| 可靠上云 | 已有 UUID 幂等、重试和隔离 | 尚未完成生产云部署和长时间断网补传验收 |
| 云端配置 | 已有可复用客户安装包、逐机配对审批、独立凭据、配置拉取、原子应用与版本回执 | Windows 使用 DPAPI；仍缺少已批准节点的自助停用/凭据轮换、正式回滚和完整审计查询 |
| Windows 自动运行 | 已有 pywin32 Windows Service、安装/卸载脚本、延迟自动启动和失败恢复配置 | 仍是需预装 Python 的试点形态；未在目标 Windows 实机验证，也没有签名 EXE/MSI、自动升级和回滚 |
| 健康与日志 | 已有本地健康接口和服务心跳，可上报设备数、最后采集、积压、磁盘、时钟偏差、版本和配置错误 | 缺少云端告警规则、一键脱敏诊断包和可验证的日志轮转策略，心跳仍需真实公网长时间验收 |
| 安全发布 | 已有白名单源码包和哈希 | 缺少 Windows 代码签名、自动升级签名和生产回滚验证 |
| 多采集器管理 | 已支持通用包自动登记多台电脑、逐机命名、设备单节点归属和 90 秒实例租约 | `sites` 暂时兼作采集器节点；真正的物理站点、多采集器主备与设备级接管仍需独立模型 |
| 远程业务闭环 | 已有实时、历史、报警、采集器清单和设置雏形 | 多角色、报警处置、通知、报表和非技术用户入口仍不完整 |

因此，当前代码可作为技术原型和部分核心逻辑基础，但不能直接等同于“可给非技术人员安装并长期无人值守运行的生产采集器”。

## 14. 反方检查

### 最强反对意见 1：自动扫描会不会让安装最简单？

会降低首次输入 IP 的成本，但“全自动纳管”会带来误识别、IP 漂移、跨网卡扫描和错误业务归属。更安全且同样易用的方案是自动发现候选、自动验证技术身份、人工确认业务归属。

### 最强反对意见 2：本地有 SQLite 是否就等于不会丢数据？

不是。没有保留期限、容量模型、磁盘告警、备份恢复和断电测试，SQLite 只能证明“有落盘位置”，不能证明长期可靠。

### 最强反对意见 3：为了远程方便，是否应该让云端直接访问采集器？

首版不应该。开放入站通道会把办公公网风险带入车间设备网。优先采用采集器主动上传、主动拉取配置和心跳；远程协助另做短期授权与审计。

## 15. 可复用原则

1. **端点不是身份**：适用于 IP、COM 口或网关地址可能变化的设备；只有在设备位置和地址永久固定且有人工台账时，端点才可暂时作为弱身份。
2. **先落盘再传输**：适用于网络不可靠但本地磁盘相对可靠的边缘采集；如果设备本身能可靠缓存并支持游标补读，可重新评估本地全量缓存方式。
3. **至少一次投递 + 幂等，而不是幻想恰好一次**：适用于 HTTP/MQTT 等可能超时重试的链路；只有单机事务内部才可能用更强的一致性语义。
4. **自动发现不等于自动信任**：适用于共享局域网和工业设备；在封闭、预注册证书并具有强设备身份的网络中，才可以进一步自动纳管。
5. **分别证明采集、存储、上传和展示**：适用于所有数据平台；页面出现一个数值不能反推设备读取、持久化和云端确认全部成功。
