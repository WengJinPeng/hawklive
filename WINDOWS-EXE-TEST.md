# DPC8001-G Windows EXE 实机测试说明

该测试版将 Python 解释器和运行依赖一起封装，目标 Windows 电脑不需要安装 Python。

## 启动方式

双击 `01-START-DEMO.cmd`（或直接双击 EXE）。默认进入演示模式并自动打开：

`http://127.0.0.1:8787/collector.html`

首次运行会在黑色窗口中显示一次性生成的客户登录账号和密码。关闭黑色窗口即停止便携测试版，历史数据仍保存在：

`%LOCALAPPDATA%\HawkHive\DPC8001-Collector`

真实设备测试直接双击 `02-START-DEVICE.cmd`。也可以从命令提示符启动：

```bat
HawkHive-DPC8001-Collector.exe --device
```

## 实机验收

1. 验证目标电脑没有安装 Python，EXE 仍可启动。
2. 验证采集器页面、客户工作台、大屏和中英文切换。
3. 关闭后重新启动，确认历史数据和配置仍然存在。
4. 真实设备模式下扫描 DPC8001-G，并核对设备显示值、采集器原始值和页面值。
5. 断开互联网至少 30 分钟，确认本地继续保存；恢复网络后确认积压数据补传。
6. 检查 Windows Defender、SmartScreen、杀毒软件和域策略是否拦截。

## 边界

- 这是未签名的便携测试 EXE，不是量产安装程序。
- 便携测试版需要保持控制台窗口开启，尚未作为 Windows 服务后台运行。
- 正式版仍需代码签名、安装/卸载、服务自动启动、升级回滚和权限 ACL 验收。
