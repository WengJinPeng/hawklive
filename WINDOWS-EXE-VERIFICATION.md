# DPC8001-G Windows EXE 验证记录

验证日期：2026-09-03（Asia/Shanghai）

真机协议复测：2026-09-04（Asia/Shanghai）

测试对象：`HawkHive-DPC8001-Collector-auto-discovery-win11-tested.exe`

EXE SHA-256：`d117d0a2bb91c8af786e20757656096c0af6f60e02ac4b6e39b18f4c602111fc`

## 已验证

- 在 Windows 11 `10.0.22631.7219`、x86-64 电脑上完成 PyInstaller 6.22.2 的 onedir 与 onefile 构建。
- 单文件 EXE 能启动本机采集器、绑定 `127.0.0.1:8787` 并自动打开管理页面。
- 关闭再启动后，本机 SQLite 数据和配置仍保存在 `%LOCALAPPDATA%\HawkHive\DPC8001-Collector`。
- 页面静态资源与源码 `public/collector.html` 的 SHA-256 一致。
- EXE 内已包含 `psutil`。在实机的多网卡环境中，采集器自动选择活动 WLAN `192.168.2.53`，推导并扫描 `192.168.2.0/24`，不需要人工填写设备 IP。
- 2026-09-03 的基线 EXE 自动扫描发现 `192.168.2.30:502`，但当时页面只显示“仅 TCP 可连接”；该旧结论已被下方 2026-09-04 真机协议复测取代，不能用来评价修复后的源码。
- 临时 Python 卸载后，`py --version` 返回 `No installed Python found!`；同一个单文件 EXE 再次启动成功并自动打开页面。
- 无 Python 复测使用 `--port 8789 --device`，页面由 EXE 自身启动并返回采集器状态。
- 远程电脑中的临时源码、离线轮子、Python 安装器、构建输出与测试进程已删除；`8787` 旧测试监听已释放。采集器运行数据保留，桌面原有安装包未修改。

## 2026-09-04 真机协议复测

- 设备持续在线：ICMP 无丢包，TCP `502` 可建立连接，MAC 为 `24:42:E3:4C:C8:E7`。
- 已取得严格匹配事务号的标准 Modbus TCP 响应：请求 `A5 01 00 00 00 06 01 03 00 00 00 02`，响应 `A5 01 00 00 00 07 01 03 04 03 16 00 0A`。
- Unit ID `1`、功能码 `0x03`、实时寄存器 `0-25`、粒子单位 `133`、配置寄存器 `125-134`、通讯寄存器 `148-151` 和 LAN 寄存器 `236-243` 均完成只读测试。
- 当前粒子单位为 `1 = PCS/28.3L`；六通道读数分别为 `656150 / 199130 / 43720 / 7230 / 1160 / 270`。温度约 `27.1-27.3°C`、湿度约 `76.9-79.2%RH`。
- 寄存器 `50=0`，设备当前处于停止采样；粒子值保持不变符合停采状态，未擅自发送启动写命令。
- 发现探针已修复两个真机问题：忽略旧连接遗留的不同事务号响应；按协议字段完整读取 U32 寄存器 `0-1`，不再读取半字段。
- 修复后对整个 `192.168.2.0/24` 默认扫描，可将 `192.168.2.30:502` 标记为 Modbus 响应、目标协议兼容、单位支持和可安全添加；设备身份仍需人工确认。
- 以生产后台监控服务执行隔离端到端测试：真实读数进入临时 SQLite，`source=device`、`particle_unit_code=1`、协议档案和设备/房间标识均已保存；`synced_at` 保持为空，证明没有云端时记录留在本机待传。测试后已删除临时数据库。
- 同一长连接按正式默认 10 秒节奏连续只读 12 次，12/12 成功且无错误；单轮延迟 `607-1739 ms`，中位数 `743 ms`。这属于短时稳定性证据，不替代 24 小时现场运行。
- 修复后的当前源码再次完成 122 项全量回归，全部通过。

## 未验证，不可视为通过

- 设备型号强身份：协议没有可读序列号、型号或 Device Identification 证据；当前只能确认协议与数据格式兼容，仍不能仅凭端口和寄存器宣称硬件身份已自动验证。
- 动态采样闭环：寄存器 `50=0`，本轮遵循只读边界，没有启动设备；尚未核对启动后的粒子变化与设备屏幕值。
- Windows EXE 包含的是 2026-09-03 构建；2026-09-04 的发现探针修复需要重新构建并再次执行无 Python Windows 验收。
- 云端真实上传、断网积压补传和服务端去重：未提供可用 Edge Token，未连接生产或测试云端。
- Windows Defender/SmartScreen、企业域策略、代码签名、安装卸载、Windows 服务自启动和升级回滚。

## 真机复测入口

当前设备的标准 Modbus TCP 和 Unit ID `1` 已确认。重新构建 Windows EXE 后运行：

```bat
HawkHive-DPC8001-Collector.exe --device
```

随后依次完成“自动扫描网络 → 协议握手 → 核对设备屏幕值/原始寄存器/页面值 → 断网持续采集 → 恢复网络补传”。只有该链路有设备端、页面端、本地库与云端四方证据后，才能标记真机采集与云上传通过。
